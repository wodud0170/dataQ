# 71. 셀레니움 시나리오 명세 — 30개 평문 절차 (2026-05-01 기준)

작성일: 2026-05-01
대상: `dataQ설계/테스트/selenium/test_*.py` 30개
기준 환경:
- q-center: http://localhost:28091
- q-executor: http://localhost:28098
- dataq-db: PostgreSQL, 포트 25433
- oracle-xe: Oracle 19, 포트 1521 (TESTUSER/XEPDB1)
- 관리자 계정: space/123, 일반 사용자: jyjang/123

본 문서는 각 셀레니움 파일에 대해 **목적 / 사전 조건 / 단계 / 검증 / 소요 시간**을 평문으로 풀어 정리한다.

---

## 카테고리

1. [기본 인증](#1-기본-인증)
2. [데이터 모델 — 컬럼 그리드/엑셀 업로드](#2-데이터-모델--컬럼-그리드엑셀-업로드)
3. [데이터 모델 — 테이블 변경/삭제 cascade](#3-데이터-모델--테이블-변경삭제-cascade)
4. [표준 사전 — 단어/용어 승인 워크플로](#4-표준-사전--단어용어-승인-워크플로)
5. [통합 검색](#5-통합-검색)
6. [표준 진단 / 구조 변경 진단](#6-표준-진단--구조-변경-진단)
7. [자동 표준화 변환 (컬럼 표준화)](#7-자동-표준화-변환-컬럼-표준화)
8. [진단 스케줄러 (Phase 1~4)](#8-진단-스케줄러-phase-14)
9. [데이터 품질 진단 (67/70번 — 값/룰)](#9-데이터-품질-진단-6770번--값룰)

---

## 1. 기본 인증

### test_login.py — 로그인/로그아웃
**목적**: STOMP WebSocket 기반 인증 + 세션 전환
**사전**: q-center 기동, 두 계정(space/jyjang) 존재
**단계**:
1. 로그인 화면 진입 → `space`/`123` 입력 + 로그인
2. 우측 상단 아바타 클릭 → 로그아웃
3. 다시 `jyjang`/`123` 로그인
4. `/main` 페이지 유지 확인 (로그인 화면으로 튕기면 fail)

**검증**: 두 계정 모두 메인 화면 도달 + WebSocket 세션 정상
**소요**: 64초

---

## 2. 데이터 모델 — 컬럼 그리드/엑셀 업로드

### test_53_grid_add_save.py — 그리드 인라인 편집으로 컬럼 추가 + 배치 저장
**목적**: 53번 설계의 컬럼 그리드 인라인 편집 (한글명 → 표준화 흐름)
**사전 조건**: 신규 논리 모델 / 테이블 / 컬럼 0건 시작
**단계**:
1. 로그인 (space/123)
2. API 로 논리 모델 생성
3. 테이블 추가 (한글명 1건)
4. 컬럼 화면 이동 → 추가 대상 테이블 선택
5. [+ 컬럼 추가] 3회 클릭 → 빈 행 3개 생성
6. 각 행의 inline input 에 한글명 입력 (사용자명, 고객명, 등록일시)
7. [저장] 클릭 → `/api/dm/saveAttrs` 호출
8. API 로 검증 — 컬럼 3건 등록, 영문명 = `TMP_COL_N`, `termsStndYn='N'`

**검증**: 그리드 → 백엔드 → DB 일관성. 표준 미적용 컬럼은 `TMP_COL_*` 자동 채번
**소요**: 39초

---

### test_53_paste.py — TSV 붙여넣기
**목적**: 외부 엑셀에서 복사 → 그리드에 paste 한 번으로 다중 컬럼 등록
**단계**:
1. 로그인
2. API 로 모델/테이블 준비
3. 컬럼 화면 진입 + 모델/테이블 선택
4. JS 로 paste 이벤트 dispatch — TSV 5행 (한글명·NULL·PK·FK·기본값 혼합)
5. 그리드 newRows 5개 생긴 것 확인
6. [저장] → 5건 저장 검증

**검증**: PK='Y' 행은 nullable 자동 'N' 강제, 기본값 다양 표기 (Y/TRUE/1) 모두 반영
**소요**: 22초

---

### test_53_resolve_trigger.py — 표준 변환 [선택 컬럼 물리모델 변환]
**목적**: 비표준 컬럼 → 용어사전 매칭 → 표준 영문명 자동 부여
**단계**:
1. 로그인 + 모델/테이블/컬럼 준비 (사용자명 1건만 저장)
2. 컬럼 화면에서 해당 행 체크박스 선택
3. [선택 컬럼 물리모델 변환] 클릭 → `/api/dm/resolveAttrs` 호출
4. 결과 분기:
   - 용어사전에 '사용자명'이 APRV_YN='Y' → `TERMS_STND_YN='Y'`, 영문명은 표준
   - 없으면 fail-soft → `TERMS_STND_YN='N'` 그대로 + failed list 에 포함
**검증**: 두 분기 모두 PASS 판정 (시드에 따라 결과 다름)
**소요**: 20초

---

### test_53_upload_attrs.py — 컬럼 엑셀 업로드
**목적**: 다수 컬럼 일괄 등록 (xlsx)
**단계**:
1. 로그인 + API 로 모델 + 테이블 1건 선행 생성
2. 컬럼 화면 진입
3. 테스트용 xlsx 생성 (openpyxl, 3행 — PK 1, FK 1)
4. [엑셀 업로드] → preview 다이얼로그 → [등록 실행]
5. API 검증 — 3개 컬럼 등록, `attrNm=TMP_COL_N`, PK/FK 반영

**검증**: parseAttrWorkbook 의 PK/FK/nullable/기본값 매핑 정확
**소요**: 23초

---

### test_53_upload_tables.py — 테이블 엑셀 업로드
**목적**: 다수 테이블 일괄 등록 + 양식 다운로드
**단계**:
1. 로그인 + API 로 모델 생성
2. 테이블 화면 → [양식 다운로드] xlsx 매직넘버 검증
3. 테스트용 xlsx 생성 (2행)
4. [엑셀 업로드] → preview 다이얼로그 (요약 카운트 표시) → [등록 실행]
5. API 검증 — 테이블 2개, `objNm=TMP_TBL_N`

**검증**: 다운/업로드 사이클 + commit 후 그리드 새로고침
**소요**: 23초

---

### test_attr_display_and_resolve_btn.py — 비표준 컬럼 표시 + 변환 버튼 토글
**목적**: PC1 4-27 (501d8b2) 의 컬럼 화면 UX 보강 검증
**검증 대상**:
- 비표준(`termsStndYn='N'`) 컬럼의 `attrNm` 셀이 빈값 표시 (TMP_COL_N 노출 X)
- [선택 컬럼 물리모델 변환] 버튼은 체크 안 해도 화면에 노출, 단 disabled
- 컬럼 체크 시 enabled

**단계**:
1. 로그인 + CAMS 모델 진입 (비표준 컬럼 다수 존재)
2. 컬럼 그리드의 attrNm 셀 — TMP_COL_N 표시 X 확인
3. [선택 컬럼 물리모델 변환] 버튼 visible + disabled 확인
4. 행 체크 → 버튼 enabled 전이 확인

**소요**: 19초

---

### test_attr_update_preserves_meta.py — 컬럼 인라인 UPDATE 정합성
**목적**: PC1 4-27 (818f151) — 한글명 변경 시에만 표준 강등, dataType/dataLen 보존
**배경**: 이전 버그 — 모든 UPDATE 가 `termsStndYn='N'` 강제 + dataType 빈값 덮어쓰기
**단계**:
1. 로그인 + 새 논리 모델 + 테이블 1개 + 컬럼 1개 (한글명 '고객명')
2. resolveAttrs 호출 → 표준 매칭 → `termsStndYn='Y'`, attrNm='CUST_NM', VARCHAR(100)
3. **케이스 A**: 한글명 그대로, nullableYn 만 변경 → `termsStndYn='Y'` 보존, dataType 보존 검증
4. **케이스 B**: 한글명 '고객명' → '주문일자' 변경 → `termsStndYn='N'` 강등, dataType 보존 검증
5. cleanup

**소요**: 9초

---

## 3. 데이터 모델 — 테이블 변경/삭제 cascade

### test_obj_rename_cascade.py — 물리 테이블명 변경 cascade
**목적**: 818f151 의 previewObjRename / updateObj 5단계 cascade 검증
**단계**:
1. 로그인 + 신규 논리 모델 + 테이블 (한글 '고객정보' → 자동 TMP_TBL_1)
2. API 로 컬럼 3건 추가 (cascade 영향 카운트용)
3. 테이블 메뉴 → [수정] → 물리명 input 활성 검증
4. 물리명을 `TB_CUSTOMER` 로 변경 → preview swal "컬럼 3건" 안내 확인
5. [변경] → swal 닫힘 + 그리드에 TB_CUSTOMER 표시
6. API 로 ATTR.OBJ_NM='TB_CUSTOMER' cascade 검증

**검증**: OBJ_NM rename + ATTR/INDEX/CONSTRAINT/REF_TABLE_NM 5단계 트랜잭션
**소요**: 39초

---

### test_logical_model_e2e.py — 논리 모델 풀 흐름
**목적**: DataSource 없는 논리 모델 등록 → 컬럼 표준 적용 → DDL 다운로드
**단계**:
1. 로그인 + 논리 모델 생성 (DSN 없음)
2. 테이블 추가 (논리명만)
3. 컬럼 추가 + [표준 적용] → 매칭 성공 컬럼만 저장
4. 데이터 모델 현황 → DDL 다운로드 (Oracle/PostgreSQL DDL 생성)

**전제**: TB_TERMS 에 '사용자명/고객명/등록일시' 중 최소 1건 APRV_YN='Y'
**소요**: 47초

---

## 4. 표준 사전 — 단어/용어 승인 워크플로

### test_word_approval_flow.py — 단어 등록 → 승인 → 삭제
**목적**: 일반 사용자 등록 신청 → 관리자 승인 → 삭제 풀 사이클
**단계**:
1. jyjang/123 로그인 → 단어 사전 → 단어 등록 (미승인 상태로 생성)
2. 로그아웃 → space/123 로그인 → 승인 화면 → 해당 단어 승인
3. 단어 사전 → 승인된 단어 검색 → 체크 → 삭제

**준수**: 모든 동작은 DOM 클릭만 (execute_script 로 Vue 메서드 직접 호출 금지)
**소요**: 91초

---

### test_full_approval_flow.py — 권한 + 통합 승인/반려
**목적**: 일반/관리자 권한별 UI 차이 + 단어/용어/도메인 종합 흐름
**단계**:
1. **STEP1 (jyjang)**: "등록 신청" 버튼 라벨 / 수정·삭제·일괄 비노출 확인 → 단어A·B 등록 신청 + 도메인 1건
2. **STEP2 (space)**: "등록" 라벨 / 수정·삭제 노출 → 승인 화면에서 단어A 승인 / 단어B 반려(사유 입력) / 도메인 승인
3. **STEP3 (jyjang)**: 마이페이지 요청 현황 — 승인 2건, 반려 1건 확인
4. **STEP4 (space)**: 정리 — 단어A 삭제, 도메인 삭제

**소요**: 219초 (가장 무거운 시나리오)

---

### test_cascade_and_word_first.py — 단어-용어 cascade 반려 + 단어 선승인
**목적**: 단어 반려 시 연쇄 삭제 / 용어 승인 시 단어 선승인 강제
**단계**:
1. (jyjang) 단어 A·B 등록 신청
2. (jyjang) 용어A=단어A+명, 용어B=단어B+명 등록 신청
3. (space) 단어A 반려 → 용어A 도 cascade 삭제 + 알림 메시지
4. (space) 용어B 승인 시도 → "단어를 먼저 승인" alert 표시
5. (space) 단어B 승인 → 용어B 재승인 성공
6. 정리

**소요**: 150초

---

### test_reject_physical_delete.py — 반려 시 물리삭제 + 재등록
**목적**: 단어 반려 후 TB_WORD 에서 물리 삭제 확인 + 동일 단어 재등록 가능
**단계**:
1. jyjang: 단어 등록 신청
2. space: 승인 화면에서 반려
3. 검증:
   - 단어 사전 검색에서 미노출 (TB_WORD 행 삭제됨)
   - 승인 이력(TB_APRV_STATS) 에 반려 이력 존재
   - 동일 단어명으로 재등록 가능
4. 정리

**소요**: 102초

---

## 5. 통합 검색

### test_global_search.py — 통합 검색 → 사전 이동
**단계**:
1. space 로그인
2. 상단 통합 검색바 "단어" 입력 → Enter
3. 통합검색 탭에서 "단어등록TEST" 행 클릭
4. 단어 사전 탭 자동 이동 + 검색조건 자동 세팅 + 데이터 조회 확인

**소요**: 30초

---

## 6. 표준 진단 / 구조 변경 진단

### test_diag_includes_nonstandard.py — 비표준 컬럼이 진단 대상에 포함되는지
**목적**: PC1 4-27 의 정설 — 표준 진단은 모든 ATTR (TERMS_STND_YN 무관) 대상
**배경**: e4aaeaf (4-26) 가 'Y' 만 진단 → 부작용으로 비표준 컬럼이 진단에서 빠짐 → 4-27 회수
**단계**:
1. 로그인 + cams 모델 + 첫 테이블 + 최신 CLCT 조회
2. 임시 비표준 컬럼 1개 추가 (saveAttrs)
3. 표준 진단 시작 → DONE 폴링 (180초)
4. 진단 결과 row 에 임시 컬럼 포함 확인 (TERM_NOT_EXIST 로 잡힘)
5. cleanup

**소요**: 9초

---

### test_logical_model_diag_handling.py — 논리 모델 진단 처리
**목적**: 논리 모델(DSN 없음) 의 STRUCT 진단 거부 + 표준 진단의 비표준 처리
**단계**:
- A. **표준 진단**: TERMS_STND_YN != 'Y' 컬럼은 진단 대상에서 제외 → process_cnt = count('Y')
- B. **구조 변경 진단**: 논리 모델 → 400 + "[CONFIG]" 메시지 즉시 거부
- C. **스케줄러 STRUCT 트리거** (논리 모델): LOG.execStatus='ERROR' + errorMsg startswith "[CONFIG]"

**소요**: 7초

---

## 7. 자동 표준화 변환 (컬럼 표준화)

### test_ca8858d_clsf_domain.py — 분류어 + 도메인 cascade UI
**목적**: ca8858d (4-22 PC2) — DSTermRecommend 의 분류어 autocomplete + 도메인 cascade
**단계**:
1. API 검증: `/api/std/getClassificationWords` (word_clsf_yn='Y' + domainClsfNm)
2. API 검증: `/api/std/getDomainsByClsf?domainClsfNm=수`
3. UI: 자동 표준화 지원 > 컬럼 표준화 메뉴 진입
4. textarea 에 한글 컬럼명 입력 + [분석 시작] → STEP3(리뷰) 도달
5. 리뷰 행의 [수정] 버튼 → 모달 열림
6. 모달의 분류어 autocomplete + [분류어 추가] + 도메인 v-select 존재 확인
7. 분류어 선택 → [분류어 추가] → 단어 테이블에 행 +1 검증

**핵심 fix (4-28)**: textarea send_keys 가 Edge 한글 IME 환경에서 v-model 트리거 실패 → JS prototype value setter + input 이벤트로 우회
**소요**: 23초

---

### test_term_recommend_cases.py — 자동 표준화 변환 케이스 7종 + before→after 정확 매칭
**목적**: API `/api/std/analyzeTermsBatch` 의 다양한 케이스 + 정확한 변환값 매칭
**사전 조건**: TB_WORD 에 단어 11개 (회원=MBR / 명=NM / 번호=NO / 상품=GDS / 주문=ORDR / 등록=REG / 일시=DT / 금액=AMT / 성별=GNDR / 수정=MDFCN / 코드=CD), TB_TERMS 에 용어 5개 (회원명=MBR_NM / 회원번호=MBR_NO / 상품명=GDS_NM / 주문번호=ORDR_NO / 등록일시=REG_DT)

**T1. REGISTERED 5건**: 등록 용어 정확 매칭 (회원명→MBR_NM, 회원번호→MBR_NO, 상품명→GDS_NM, 주문번호→ORDR_NO, 등록일시→REG_DT)
**T2. AUTO 5건**: 단어 결합 자동 영문 약어 (회원금액→MBR_AMT, 주문상태→ORDR_STTS, 상품가격→GDS_PRC, 회원성별→MBR_GNDR, 주문수정일시→ORDR_MDFCN_DT)
**T3. PARTIAL**: '회원이름' / '주문나이' — 알려진 단어 MATCHED + '이름'/'나이' NEW
**T4. FAILED**: '블라블라' / '라라라' — 미등록 단어만, MATCHED ≤ 1
**T5. 영문 단어**: 'API번호' / 'CMS코드' — 영문 단어가 MATCHED
**T6. 13건 한 번 분석**: status 분포 (REGISTERED 3 / AUTO 6 / PARTIAL 2 / FAILED 2)
**T7. UI 풀 흐름**: 컬럼 표준화 메뉴 진입 → textarea 5건 입력 → [분석 시작] → 결과 그리드 5+ 행

**소요**: 21초

---

## 8. 진단 스케줄러 (Phase 1~4)

### test_phase1_schedule_api.py — 진단 스케줄러 API 스모크
**API 검증**: create / list / detail / update / toggle / cronPreview / runNow / logs / delete + 비관리자 403
**전제**: TB_DATA_MODEL 1건 이상
**소요**: 1초 (API 만)

---

### test_phase2_schedule_exec.py — 스케줄러 실제 실행 통합
**검증**:
- A. runNow → q-executor 거쳐 실제 DiagService/StructDiagService 기동
- B. 완료 폴링 → LOG 가 DONE 또는 ERROR 마감
- C. SCHEDULE.LAST_EXEC_DT/STATUS 갱신

**전제**: q-center + q-executor 기동, TB_DATA_MODEL 1건. 수집 이력 없으면 [DATA_NOT_FOUND] ERROR 마감 검증
**소요**: 26초

---

### test_phase3_schedule_ui.py — 진단 스케줄러 UI 스모크
**단계**:
1. 관리자 로그인
2. 진단 스케줄 그룹 → 스케줄 관리 진입 → DSScheduleManage 렌더 확인
3. [스케줄 추가] → SIMPLE 등록
4. 목록 행 + 활성 스위치 ON 확인
5. [즉시 실행] → swal → 완료 대기
6. 진단 스케줄 그룹 → 스케줄 실행 이력 → 방금 로그 행 표시
7. 로그 행 클릭 → drawer 상세
8. 정리 (스케줄 삭제)

**핵심 fix (4-28)**: PC1 4-27 메뉴 분리 (관리 그룹 → scheduleGroup 별도 대그룹) — `mmGroup` → `scheduleGroup` 으로 nav id 갱신
**소요**: 37초

---

### test_phase4_schedule_coverage.py — Phase 4 커버리지 보강 (API)
**커버**:
- A. SIMPLE 자동 트리거 (REPEAT_TIME = 현재+1분, 90초 내 AUTO 이력)
- B. STRUCT 진단 경로 — runNow 시 structDiagId LOG 세팅
- C. STANDARD 실패 prefix — 수집 없음 → [DATA_NOT_FOUND]
- D. scheduleType 전환 — SIMPLE ↔ CRON update 왕복
- E. SKIP 정책 — runNow RUNNING 상태에서 자동 트리거 → SKIPPED 이력
- F. BOTH (STANDARD + STRUCT 병행) — 둘 다 DONE 시 LOG DONE

**소요**: 248초 (가장 긴 — A 가 60~90초 대기 필요)

---

### test_phase4_ui_admin_gate.py — 비관리자 권한 게이트
**시나리오**:
1. jyjang 로그인
2. "관리" / "진단 스케줄" 대메뉴 v-if="isAdmin" 으로 숨겨짐
3. 혹시 렌더돼도 nav_scheduleManage / nav_scheduleLog 미노출

**소요**: 10초

---

## 9. 데이터 품질 진단 (67/70번 — 값/룰)

### test_qual_smoke.py — 67번 골격 스모크 (9 STEP)
**시나리오**: 카탈로그 → 룰 등록 → 룰 진단 → 결과 → 값 프로파일링 → UI 메뉴 진입
**단계**:
1. 로그인
2. CAMS 모델 + 첫 OBJ + 첫 ATTR 자동 선택
3. 카탈로그(NOT_NULL) → 룰 등록
4. 룰 list 조회 → 등록 확인
5. 룰 진단 실행 (sampleRate=10) → DONE 폴링
6. 룰 결과 조회 (요약)
7. 값 프로파일링 실행 → 결과 조회
8. UI: [데이터 품질 진단] 그룹 → [업무 규칙 관리] → 룰 추가 버튼 visible
9. 정리 (soft-delete)

**소요**: 27초

---

### test_qual_scenario.py — 67번 풀 시나리오 (6 STEP)
**모델**: TEST_QUAL_MODEL (Oracle TESTUSER, 139 행)
**단계**:
- A. 모델 + 룰 메타 사전 검증 (10+ 룰)
- B. 값 진단 풀스캔 + 22 컬럼 통계 적재 검증 (EMAIL total=55 null=5)
- C. 룰 진단 풀스캔 + DONE 마감
- D. 예상 위반 vs 실제 위반 비교 (16개 룰 매칭)
- E. 종합 요약

**소요**: 11초

---

### test_qual_70_scenario.py — 70번 도메인 룰 1:N + 컬럼 매핑 (8 STEP)
**검증 흐름**:
- G1. effective rule 매퍼 — 22 컬럼 × {DOMAIN/CUSTOM/EXCLUDED/DEFAULT/NONE}
- G2. 모델 단위 진단 → PHONE 컬럼 default('-' 패턴) 위반 카운트
- G3a. PHONE 컬럼 룰을 SORT_ORD=2 (NODASH '01012345...') 로 매핑 변경
- G3b. **그 컬럼 1개만 재진단** → 위반 카운트 변화 검증 (단위 격리)
- G4. 진단 제외 토글 (EXCLUDE_YN='Y')
- G5. HISTORY 누적 → trend API 시계열 행 추가
- G6. UI 메뉴 — 컬럼 규칙 매핑 + 진단 통계

**소요**: 33초

---

### test_qual_value_v2.py — 70번 새 화면 (DSQualValueProfile 재설계, 8 STEP)
**API + UI 통합**:
- H1. listWithLatest API — effective + 직전 PROFILE/RULE join
- H2. value/runColumns API — 3 컬럼 다중 진단 + 결과 적재
- H3. rule/runColumns API — 2 컬럼
- H4. UI: 모델 선택 → 그리드 표시 + 체크박스
- H5. UI: 테이블 필터로 행 좁히기
- H6. UI: 선택 후 [선택 컬럼 프로파일링] → swal
- H7. UI: [상세] 버튼 → drawer 팝업

**소요**: 39초

---

### test_qual_ui_full.py — UI 풀 검증 (20 STEP, 시나리오 6종)
**시나리오**:
- A. 업무 규칙 관리 (메뉴 진입 + 모델 선택 + 16+ 행 + 8가지 RULE_TYPE 6+ 표시)
- B. 값 프로파일링 (메뉴 → 컬럼 그리드 → [전체선택] → 시작 → 30초 후 % 표시)
- C. 업무 규칙 진단 결과 (API 트리거 → diagId 입력 → 16+ 행 + 위반률 표시)
- D. 룰 직접 등록 (다이얼로그 → 폼 → 저장 → 그리드 +1)
- E. 카탈로그 가져오기 (다이얼로그 → 가져오기 → 그리드 +1)
- F. 룰 관리에서 [진단 실행] (잔여 dialog 정리 후 클릭 → swal)

**핵심 fix (4-30)**: DSQualValueProfile 재설계 후 selector 갱신 (`btn-value-run` → `btn-run-selected`)
**소요**: 124초

---

### test_qual_user_scenarios.py — 사용자 시나리오 5종 무거운 검증 (30 STEP)
**S1. 신규 사용자 풀 흐름 (9 STEP)**:
- UI 로그인 → 값 프로파일링 화면 → DASH 진단
- DB 직접 query 로 카운트 일치 확인
- 컬럼 매핑 화면 → PHONE 룰 NODASH 변경
- 재진단 → 적합률 50%p+ 차이 + 위반 20+ 증가 검증
- 진단 통계 시계열 2+ row 확인
- 원복

**S2. 도메인 룰 우선순위 (5 STEP)**:
- DASH/NODASH/AREA 3룰 시계열 진단
- 적합률 다름 + DASH 우세
- DB HISTORY 3+ 누적
- trend API 3+

**S3. 진단 단위 격리 (5 STEP)**:
- 모델 전체 진단 → DB snapshot
- EMAIL 1개만 재진단
- DB snapshot 비교 → **EMAIL 만 변경 + 나머지 21 컬럼 100% 보존** 검증

**S4. 커스텀 룰 + 에러 케이스 (5 STEP)**:
- ruleType 누락 → 400
- 정상 등록 / AGE 매핑 / 위반 카운트 범위 검증
- 룰 삭제 + 매핑 원복
- dmId/targets 누락 → 400

**S5. UI 다중 선택 + drawer (6 STEP)**:
- UI 로그인 → MEMBER 필터 → 전체선택 → 30초 후 적합률 갱신 + API 일치
- [상세] drawer 라벨 3개 보임

**소요**: 157초

---

## 부록 A. 공통 셀레니움 패턴 (이번 세션에서 정착)

| 문제 | 우회 |
|---|---|
| `v-list-group` root `<div>` click 이 펼침 안 됨 | `v-list-group__header` activator 클릭 |
| 그리드 cell `.text` 가 viewport 밖 못 가져옴 | JS `execute_script` 로 textContent 직접 추출 |
| swal `.swal2-popup` visibility wait timeout | body 의 `swal2-shown` class 폴링 |
| 화면 하단 버튼 `element_to_be_clickable` timeout | `presence_of` + `scrollIntoView` + JS click |
| swal2 input `send_keys` ElementNotInteractable | JS prototype value setter + `input` event dispatch |
| Edge 한글 IME textarea send_keys 실패 | JS prototype setter (`HTMLTextAreaElement.prototype.value`) |

## 부록 B. 사전 조건 시드 SQL

| SQL | 용도 |
|---|---|
| `dataQ설계/테스트/sql/oracle/01_qual_test_ddl_oracle.sql` | Oracle TESTUSER 의 테스트 테이블 3종 DDL |
| `dataQ설계/테스트/sql/oracle/02_qual_test_data_oracle.sql` | 139행 시나리오 데이터 INSERT |
| `dataQ설계/테스트/sql/oracle/03_qual_test_metadata_pg.sql` | dataQ 메타 (DSN/모델/OBJ/ATTR) |
| `dataQ설계/테스트/sql/70_qual_test_seed_pg.sql` | 70번 도메인 룰 4 + 커스텀 룰 10 + 컬럼 매핑 22 |

## 부록 C. 실행 명령

전체 일괄:
```bash
bash /c/temp/run_all_selenium.sh
# 결과: /tmp/selenium_results.txt + /tmp/sel_*.log
```

개별:
```bash
cd "dataQ설계/테스트/selenium"
python test_qual_user_scenarios.py
```

## 변경 이력
- 2026-05-01 30개 시나리오 평문 절차 정리 (전수 PASS 기준)
