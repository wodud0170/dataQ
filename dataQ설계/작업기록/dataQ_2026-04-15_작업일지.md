# dataQ 작업일지 - 2026-04-15

**작업 환경**: PC1 (메인 개발환경)
**세션**: 구조 변경 진단 + 승인 프로세스 재설계

---

## 1. 구조 변경 진단 세션 (오전)

### 인덱스/제약조건 수집 현황 분석
- 현재 수집 구조 분석 (테이블/컬럼만 수집, 인덱스/제약조건 미수집)
- Oracle PK/FK 쿼리가 USER_CONSTRAINTS 사용 (타 스키마 수집 시 누락) 이슈 발견
- PostgreSQL Named Query 부재 확인
- 33_인덱스_제약조건_수집_현황분석.md 작성

### 구조 진단 결과 엑셀 다운로드
- CSV → POI xlsx 변경 (ExcelDownloadService + StructDiagController)
- 화면 그리드에 Nullable 컬럼 추가
- 이전/현재 헤더 색상 구분 (주황/파랑)

### 인덱스/제약조건 수집 구현
- DDL: TB_DATA_MODEL_INDEX, TB_DATA_MODEL_CONSTRAINT 생성
- Named Query: OracleGetIndexes, OracleGetConstraints 추가
- Oracle PK/FK 서브쿼리 USER_CONSTRAINTS → ALL_CONSTRAINTS 수정
- DataModelService 수집 루프 확장 (INDEX/CONSTRAINT 단계 추가)
- 시스템 테이블 필터 추가 (TABLESPACE_NAME NOT IN ('SYSTEM','SYSAUX'))
- CHECK 제약조건 SEARCH_CONDITION 컬럼 추가

### 데이터 모델 메뉴 확장
- DSDatamodelStatusIndex.vue 신규 (인덱스 조회 화면)
- DSDatamodelStatusConstraint.vue 신규 (제약조건 조회 화면)
- NdNav/NdContent 메뉴 등록

### 구조 진단 인덱스/제약조건 비교 확장
- TB_STRUCT_DIAG_INDEX_DETAIL, TB_STRUCT_DIAG_CONSTRAINT_DETAIL 신규
- TB_STRUCT_DIAG_HISTORY 통계 컬럼 8개 추가
- StructDiagService.run() 인덱스/제약조건 diff 로직 (toIndexMap, toConstraintMap)
- DSStructDiagResult.vue 3탭 분리 (컬럼/인덱스/제약조건)
- DSStructDiag.vue 이력 변경건수 3분할 (컬럼/인덱스/제약조건)
- 탭별 엑셀 다운로드 API 3개

### 설계 문서
- 34_구조진단_인덱스_제약조건_확장_설계.md (별도 테이블 분리 방식)
- 35_재택근무_개발일정_보고서.md
- 36_진단스케줄러_설계.md
- 37_분석보고서출력_설계.md

### 기타
- FROM DUAL 14건 전부 제거 (PostgreSQL 호환)
- .gitignore build/ → /build/ 수정 (vue/front/build 추적)
- DB 백업 + Docker 이미지 생성 (dataq-db)

---

## 2. 승인 프로세스 재설계 (오후)

### APRV_YN 정상화
- ExcelUploadService: 일괄등록 시 무조건 Y → 관리자=Y, 일반=N 분기
- sysinfo.xml에 selectIsAdmin 쿼리 추가 (q-common에서 관리자 여부 조회)

### MMApproval.vue 버그 수정
- 상세보기-일괄처리 충돌 해소 (selectedItem 덮어쓰기 제거)
- 상세 패널 닫기(X) 버튼 추가
- 검토중(1) 상태 제거 (4단계 → 승인대기/승인/반려 3단계)
- 상태 라벨 "요청" → "승인대기"

### TB_CHANGE_HISTORY 확장
- CHANGE_SOURCE 컬럼 추가 (ADMIN_DIRECT/USER_REQUEST/ADMIN_APPROVE 등)
- changehistory.xml INSERT/SELECT 반영
- DSChangeHistory.vue 등록 경로 필터 + 그리드 컬럼 추가

### MMUser.vue 전면 재작성
- 탭 구조 제거 (비밀번호 변경은 마이페이지로 이관)
- 목록-상세 구조 (좌측 목록 + 우측 상세)
- 권한 토글, 비밀번호 초기화, 계정 삭제

### LoginController 수정
- createUser/updateUser에 @RequestBody 추가 (JSON 바인딩)

### DSMyProfile.vue 수정
- DB 직접 조회로 변경 (세션 캐시 → getUserDetail API)
- FormData → JSON 전환
- autocomplete="off" (브라우저 자동완성 방지)

### NdNav.vue 권한 제어
- 관리 메뉴 v-if="isAdmin" (일반 사용자에게 숨김)
- isAdmin API 호출 + axios import 추가

### NdHaeder.vue 로그인 정보 표시
- "로그인 사용자 : {userId} 님 [관리자/일반]" 헤더 표시

### DSWord.vue 개선
- 형식 단어 여부 (?) 툴팁 추가
- 이음동의어/금칙어 (?) 툴팁 + 타이틀 "목록" 제거
- 형식 단어 Y일 때만 도메인 분류명 표시 (v-if), 필수 표시(*)
- 형식 단어/공통표준여부 default N으로 변경 (checked + reset 수정)
- form.reset() 제거 → 직접 초기화 (열기/닫기 반복 시 값 꼬임 해결)
- 형식단어 Y + 도메인 분류명 없으면 등록 차단 (단건/일괄 모두)

### 승인 안내 메시지
- 단어/용어/도메인 등록 시 일반 사용자면 "관리자 승인 후 조회 가능합니다." 안내

### 승인 프로세스 재설계 문서
- 38_승인프로세스_재설계.md

---

## 수정 파일 목록

| 파일 | 수정 내용 |
|------|----------|
| ExcelUploadService.java | 일괄등록 APRV_YN 관리자 분기 + 형식단어 도메인분류 체크 |
| DataStandardController.java | 승인 안내 메시지 + 형식단어 도메인분류 체크 |
| LoginController.java | @RequestBody 추가 |
| MMApproval.vue | 버그 3건 수정 + 검토중 제거 |
| MMUser.vue | 전면 재작성 (목록-상세) |
| DSMyProfile.vue | DB 직접 조회 + JSON 전환 |
| DSChangeHistory.vue | CHANGE_SOURCE 필터/컬럼 |
| DSWord.vue | 형식단어/도메인분류 UX + 툴팁 + default N |
| DSTerm.vue | 승인 안내 메시지 |
| DSDomain.vue | 승인 안내 메시지 |
| NdNav.vue | 관리 메뉴 권한 제어 |
| NdHaeder.vue | 로그인 사용자/권한 표시 |
| changehistory.xml | CHANGE_SOURCE 추가 |
| sysinfo.xml | selectIsAdmin 추가 |
| DDL_claude_generated.sql | CHANGE_SOURCE 컬럼 |
