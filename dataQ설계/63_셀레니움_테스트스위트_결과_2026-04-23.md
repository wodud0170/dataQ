# 63. Selenium 테스트 스위트 전체 결과 (2026-04-23)

## 환경

| 항목 | 값 |
|---|---|
| 실행 일시 | 2026-04-23 19:00 KST |
| git HEAD | `ccf76c4` (58번 빌드 절차 reactor 교정 + ca8858d 분류어/도메인 cascade E2E) |
| 빌드 방식 | **루트 reactor** `mvn clean package -DskipTests` (58번 문서 수정 후) |
| q-center jar | `target/q-center-0.0.1-SNAPSHOT.jar` (204 MB, `BOOT-INF/lib/korean-text-4.4.jar` 포함 확인) |
| q-executor jar | `target/q-executor.jar` (185 MB) |
| 서버 | q-center `localhost:28091` (200 OK), q-executor `localhost:28098` (404 — 루트 미정의, 정상) |
| DB | docker `dataq-db` (PostgreSQL 13, 외부 25433, schema `quality`) — 사용자가 직전에 **타겟 DB 도 추가 기동** |
| 테스트 실행자 | `python test_*.py` 순차, 각 로그는 `/tmp/suite_logs/*.log` |

---

## 요약

**13개 중 9개 PASS / 4개 FAIL**

| # | 테스트 파일 | 결과 | 단계 | 비고 |
|---|---|---|---|---|
| 1 | `test_login.py` | ✅ **PASS** | 3/3 | space/jyjang 로그인 + 로그아웃 전체 통과 |
| 2 | `test_53_grid_add_save.py` | ✅ **PASS** | 5/5 | 논리모델 컬럼 그리드 직접 추가 + 저장 |
| 3 | `test_53_paste.py` | ✅ **PASS** | 6/6 | TSV 5행 붙여넣기 (블록편집) |
| 4 | `test_53_resolve_trigger.py` | ✅ **PASS** | 4/4 | 한글명 resolve 트리거 |
| 5 | `test_53_upload_attrs.py` | ❌ **FAIL** | step 4 | **preview 다이얼로그 오픈 실패** |
| 6 | `test_53_upload_tables.py` | ✅ **PASS** | 6/6 | 테이블 엑셀 업로드 |
| 7 | `test_ca8858d_clsf_domain.py` | ✅ **PASS** | 10/10 | 분류어 autocomplete + 도메인 cascade (신규) |
| 8 | `test_cascade_and_word_first.py` | ✅ **PASS** | 12/12 | 용어 cascade 반려 + 단어선승인 |
| 9 | `test_full_approval_flow.py` | ✅ **PASS** | 15/15 | 전체 승인 플로우 |
| 10 | `test_global_search.py` | ✅ **PASS** | 5/5 | 통합 검색 → 단어 탭 이동 |
| 11 | `test_logical_model_e2e.py` | ❌ **FAIL** | step 4-5 | **용어사전 매칭 0건 (TB_TERMS 확인 필요)** |
| 12 | `test_reject_physical_delete.py` | ❌ **FAIL** | 5/6 | **STEP2-2 반려 처리** (나머지 5건 PASS) |
| 13 | `test_word_approval_flow.py` | ❌ **FAIL** | STEP 1 | **등록 버튼 못 찾음** |

---

## 실패 4건 상세

### 5. `test_53_upload_attrs.py` — preview 다이얼로그 오픈 실패

**로그 요지**:
```
STEP: 4. UI 업로드 → preview → commit
  [SHOT] 04a_model_selected
  [SHOT] 04b_preview_dialog
  >> FAIL: preview 다이얼로그 오픈 실패
```

**정황**:
- xlsx 파일 생성까지 성공 (step 3 PASS).
- 모델 선택 후 [엑셀 업로드] → 파일 선택 후 preview 다이얼로그가 뜨기 전에 타임아웃.
- 같은 날 `test_53_upload_tables.py` 는 **통과** — attr 쪽만 고장.

**추정 원인 (검증 필요)**:
- `DSDatamodelStatusColumn.vue` 의 파일 업로드 후 preview 다이얼로그 트리거 로직이 어긋났을 가능성.
- 백엔드 `parseAttrWorkbook` 가 예외로 빠지면서 swal 알림으로 막혔을 수 있음 → 로그에서 `/api/dm/parseAttrUpload` 류 요청 응답 확인.

**스크린샷**: `dataQ설계/테스트/selenium/screenshots/e2e_04a_model_selected.png`, `e2e_04b_preview_dialog.png`, `e2e_99_final.png`.

---

### 11. `test_logical_model_e2e.py` — 용어사전 매칭 0건

**로그 요지**:
```
STEP: 4-5. 컬럼 추가 + 표준 적용(용어/도메인 자동)
  >> FAIL: 용어사전 매칭 0건 — 후보(['사용자명', '고객명', '등록일시', '주소', '전화번호'])
           모두 표준 적용 실패. TB_TERMS 확인 필요
```

**정황**:
- 모델 생성 + 테이블 추가 (step 1~3) 모두 PASS.
- 컬럼 추가 단계에서 5개 한글명(`사용자명`/`고객명`/`등록일시`/`주소`/`전화번호`) 모두 표준 변환 실패.
- 테스트 메시지가 "TB_TERMS 확인 필요" 로 명시.

**추정 원인**:
- 테스트 환경의 `TB_TERMS` 에 위 5개 한글명이 승인 상태(`APRV_YN='Y'`) 로 **등록돼 있지 않음**.
- 또는 resolveStandard 로직이 CLCT 폐기 이후 변경된 영향으로 매칭 경로가 달라짐.

**조사 지시**:
```sql
SELECT TERMS_NM, APRV_YN, USE_YN FROM TB_TERMS
 WHERE TERMS_NM IN ('사용자명','고객명','등록일시','주소','전화번호');
```

---

### 12. `test_reject_physical_delete.py` — 5/6 (STEP2-2 실패)

**로그 요지**:
```
PASS: STEP1-1: 단어 등록 신청
PASS: STEP2-1: 승인 목록에서 단어 발견
FAIL: STEP2-2: 반려 처리 완료       ← 여기만 FAIL
PASS: STEP3-1: 반려된 단어 물리 삭제 (단어 사전에서 미노출)
PASS: STEP3-2: 승인 이력에 반려 기록 존재
PASS: STEP3-3: 반려 후 동일 단어명 재등록 존재
```

**정황**:
- STEP2-2 실패에도 뒤따르는 STEP3-1~3-3 은 모두 통과 → 결과적으로 "반려 → 물리삭제 → 이력저장 → 재등록" 기능은 동작.
- 즉 **기능은 살아있는데 반려 버튼 클릭 또는 UI 완료 신호 탐지에만 실패**한 것으로 추정.

**추정 원인**:
- 승인 관리 화면 UI 변경 (버튼 라벨/셀렉터 이동) 가능성.
- swal confirm 다이얼로그 흐름이 바뀌었을 수 있음.

**스크린샷**: `step2_reject.png`, `step3_reject_history.png`.

---

### 13. `test_word_approval_flow.py` — 등록 버튼 못 찾음

**로그 요지**:
```
[STEP 1] jyjang 로그인 → 단어 등록
  [jyjang] 로그인 성공
  그룹 펼침: 데이터 표준 사전
  메뉴 클릭: 단어 (#nav_word)
  [FAIL] 등록 버튼 못 찾음
  스크린샷: step1_no_add_btn.png
```

**정황**:
- `jyjang` 계정으로 단어 메뉴까지 진입 성공.
- 단어 리스트 화면에서 "등록" 버튼 탐색 실패 → 첫 스텝부터 중단.

**추정 원인**:
- 테스트가 기대하는 버튼 텍스트/셀렉터가 현재 UI 와 불일치.
- `jyjang` 계정이 일반 사용자로 보이는데, 일반 사용자에게도 "등록" 버튼이 뜨는 게 맞는지 확인 필요 (승인 플로우 설계상 일반 사용자도 등록 신청 가능).

**연관**: `test_cascade_and_word_first.py` 와 `test_full_approval_flow.py` 는 같은 메뉴에서 등록 버튼을 찾아 PASS 함 → **`test_word_approval_flow` 의 셀렉터만 다르거나 stale** 일 가능성 높음.

---

## 통과 9건 간단 요약

- **로그인/세션** (test_login): space/jyjang 두 계정 + 로그아웃 + 재로그인 정상.
- **데이터모델 관리** (test_53_grid_add_save, test_53_paste, test_53_resolve_trigger, test_53_upload_tables): 논리모델 컬럼 추가 (그리드 직접/TSV 붙여넣기/resolve 트리거/테이블 엑셀 업로드) 전부 정상. **`upload_attrs` 만 회귀.**
- **분류어 cascade 신기능** (test_ca8858d_clsf_domain): 545개 분류어 로드 + 도메인 cascade + `[분류어 추가]` 후 단어 테이블 행 증가까지 10/10.
- **승인 프로세스** (test_cascade_and_word_first, test_full_approval_flow, test_reject_physical_delete 중 5/6): cascade 반려/단어선승인/도메인 승인 등 기능적으로는 전부 동작 중 (reject 테스트 하나만 UI 탐지 실패).
- **통합 검색** (test_global_search): 단어 검색 결과 → 단어 탭 이동 + 필터 전달까지 전체 통과.

---

## 다음 작업 우선순위

| 우선 | 대상 | 할 일 |
|---|---|---|
| P0 | `test_53_upload_attrs` | preview 다이얼로그 경로가 실제 UI 에서도 깨졌는지 수동 확인. 백엔드 에러면 서버 로그 + parseAttrWorkbook 재현. UI 전용이면 테스트 셀렉터 갱신 |
| P0 | `test_word_approval_flow` | 테스트 셀렉터만 stale 일 가능성 큼. `test_cascade_and_word_first` / `test_full_approval_flow` 와 비교해 셀렉터 맞추기 |
| P1 | `test_reject_physical_delete` STEP2-2 | 나머지 5건 PASS 라 UI 탐지만 문제 — swal confirm / 반려 모달 셀렉터 확인 |
| P1 | `test_logical_model_e2e` | 테스트 데이터 셋업 — `TB_TERMS` 에 `사용자명/고객명/등록일시/주소/전화번호` 5건 등록 필요. 데이터 이슈이면 setup 스크립트 추가 고려 |

---

## 재실행 방법

```bash
# 서버가 내려가 있다면 먼저 기동 (58번 문서 3절)
# 이후 전체 스위트:
cd dataQ설계/테스트/selenium
for t in test_login test_53_grid_add_save test_53_paste test_53_resolve_trigger \
         test_53_upload_attrs test_53_upload_tables test_ca8858d_clsf_domain \
         test_cascade_and_word_first test_full_approval_flow test_global_search \
         test_logical_model_e2e test_reject_physical_delete test_word_approval_flow; do
  python "${t}.py" > "/tmp/suite_logs/${t}.log" 2>&1
  grep -E "총 [0-9]+: PASS" "/tmp/suite_logs/${t}.log" | tail -1
done
```

개별 테스트 로그는 `/tmp/suite_logs/{테스트이름}.log` 에 전량 저장. 스크린샷은 `dataQ설계/테스트/selenium/screenshots/` 에 각 테스트 prefix 별로 저장됨.
