# 87. 차후 과제 — 시연 이후 정비 항목

**작성일**: 2026-05-10
**컨텍스트**: 종합 시연 준비 과정에서 사용자(장재영) 가 발견·제안한 개선/정비 항목 누적
**상태 표기**: 🔵 백로그 · 🟡 진행중 · ✅ 완료 · 🟢 보류

각 항목의 **"사용자 원문"** 은 발견 당시의 표현을 그대로 보존 — 의도 왜곡 방지.

---

## #87-1. 용어 등록 모달의 동의어 입력 정책 정립 🔵

### 사용자 원문 (2026-05-10)
> "지금 용어사전 → 등록 때 동의어를 입력하면 분석이 되는데, 동의어도 이미 있으면 그냥 이미 있는 용어로 넘겨야 하는 건가?"

### 발견 컨텍스트
- 시연 대본 § 3-2 (용어 등록 v2 자동 분석) 검토 중
- 현재 등록 모달에서 한글 용어명 입력 시 1초 디바운스 후 `analyzeTermsBatch` 자동 호출
- 분석 결과로 단어 분리 + 영문약어 합성됨
- 그러나 사용자가 입력한 한글 용어명이 **이미 등록된 용어** 또는 **이미 등록된 단어의 동의어 (synonym)** 와 일치할 때 동작이 모호

### 화면 / 위치
- `데이터 표준 사전 > 용어 > [등록]` 모달
- 컴포넌트: `q-center/vue/front/src/components/DSTerm.vue`
- 분석 API: `/api/std/analyzeTermsBatch`

### 현재 동작
- 입력값에 대해 `analyzeTermsBatch` 가 토큰 분리 + 매칭만 수행
- 입력 그 자체가 등록된 용어/동의어인지 별도 lookup 없음
- 사용자가 [등록] 누른 시점에 `selectTermsByNm` 으로 unique 검사만 함 → "이미 등록된 용어명" swal

### 정책 후보
| 안 | 동작 | 장단점 |
|---|---|---|
| (a) **이미 등록된 용어/동의어 안내 + 사용자 선택** | 입력 1초 후 lookup → 일치 시 "이미 등록된 용어 / 동의어 — '%s' 용어를 사용하시겠습니까?" 알림 | 안전. 사용자 의도 확인. 권장 |
| (b) 자동 redirect | 일치 시 자동으로 그 용어 화면으로 이동 / 모달 닫힘 | UX 점프 — 의도와 다를 수 있음 |
| (c) 동의어로 그대로 입력 허용 | 사용자 입력값을 동의어 컬럼에 추가 + 별도 처리 | 동의어/용어 경계 모호 |

### 권장: (a)
- 분석 진입 시점에 입력값에 대해 `selectTermsByNm` + `selectTermsBySynonym` (신규 매퍼) 동시 호출
- 일치 시 분석 결과 위에 회색 배너로 "이미 등록된 용어/동의어입니다 — 이 용어로 가시려면 [이동]" 노출
- 분석은 그대로 진행 (사용자가 무시하고 신규 등록할 가능성도 허용)

### 영향도
- 신규 매퍼 1개 (`selectTermsBySynonym` — `ALLOPH_SYNM_LST` 배열 contains 검색)
- DSTerm.vue 분석 결과 영역 위에 안내 배너 추가
- 백엔드 endpoint 신규 또는 `analyzeTermsBatch` 응답에 `existingMatch` 필드 추가

### 추정
- 백엔드 + 프론트 — 반나절

---

## #87-2. 도메인 등록 데이터 타입 통합 🔵

### 사용자 원문 (2026-05-10)
> "도메인이 갖고있는 데이터타입은 varchar2 string 등을 varchar로 통합하는 뭐 그런 처리를 해야 할 것 같음. 도메인을 등록할 때 드롭다운 해보면 varchar2 등 쓸데없이 다른 것들이 생김."

### 발견 컨텍스트
- 시연 중 도메인 등록 화면에서 데이터 타입 드롭다운 확인
- VARCHAR / VARCHAR2 / STRING 등 사실상 동일한 타입이 분리되어 노출
- 외부 DBMS (Oracle 의 VARCHAR2, MySQL 의 STRING/TEXT) raw 타입이 도메인 사전 안으로 그대로 침투

### 화면 / 위치
- `데이터 표준 사전 > 도메인 > [등록]` 모달의 데이터 타입 드롭다운
- 컴포넌트: `DSDomain.vue`
- 백엔드: `selectDataTypeList` 류 매퍼 (TB_DOMAIN.DATA_TYPE distinct)

### 현재 동작
- 도메인 사전의 DATA_TYPE 컬럼 distinct 값을 그대로 드롭다운에 노출
- 외부 DBMS 의 raw 타입 (VARCHAR2 / NUMBER / NVARCHAR2 등) 이 누적되면서 옵션 폭증

### 제안
1. **DataQ 표준 데이터 타입 17종 정의** — 컬럼 화면 v-combobox 와 동일한 표준
   `VARCHAR / CHAR / TEXT / CLOB / BLOB / NUMBER / NUMERIC / DECIMAL / INTEGER / BIGINT / SMALLINT / FLOAT / DOUBLE / DATE / TIMESTAMP / TIME / BOOLEAN`
2. **수집 단계 매핑 테이블** — DBMS dialect 별 raw 타입 → DataQ 표준 타입
   - Oracle: VARCHAR2 → VARCHAR / NUMBER → NUMERIC / DATE → TIMESTAMP / NVARCHAR2 → VARCHAR ...
   - MySQL: STRING / TINYTEXT → TEXT / DATETIME → TIMESTAMP ...
   - PostgreSQL: bigint → BIGINT / numeric → NUMERIC ...
3. **마이그레이션** — 기존 도메인 row 의 비표준 DATA_TYPE 값을 표준으로 일괄 변환

### 영향도
- 컬럼 그리드와 도메인 등록 모달의 드롭다운 옵션 통일
- 수집 시 dialect 별 매핑 적용 (DataModelService 또는 신규 TypeMapper)
- 기존 데이터 일회성 마이그레이션 (UPDATE TB_DOMAIN SET DATA_TYPE = 매핑값)

### 추정
- 매핑 테이블 + UI 정리 + 마이그레이션 — 1~2일

---

## #87-3. 컬럼 편집의 PK/FK 입력 시 인덱스·제약조건 자동 생성 🔵

### 사용자 원문 (2026-05-10)
> "컬럼에서 FK를 다른 테이블로 참조해서 등록할 수 있고, FK 명도 등록해서 지정하면 제약조건에 그게 추가되는 식.
> PK도 마찬가지, PK 이름을 지정해서 저장하면 인덱스와 제약조건에 추가되는 식으로
> 나중에 모델링을 정말 이걸로도 할 수 있게 하고 싶어."

### 발견 컨텍스트
- 시연 대본 § 4 (논리 모델 — 직접 입력) 작성 중
- 현재 컬럼 그리드의 PK / FK 체크박스만 토글 가능 → 인덱스/제약조건 테이블에는 row 자동 생성 없음
- DataQ 단독으로 ERwin 없이도 모델링까지 완결하고 싶다는 비전

### 화면 / 위치
- `데이터 모델 > 컬럼` 그리드
- 백엔드: `saveAttrs` UPDATE/ADD 분기
- 매퍼: `tb_data_model_index`, `tb_data_model_constraint` cascade insert

### 현재 동작
- ATTR 행의 PK_YN, FK_YN, FK_PARENT_OBJ_NM, FK_PARENT_ATTR_NM 만 저장
- 인덱스/제약조건 테이블에는 row 자동 추가 없음 → DDL 다운로드 시 PK/FK 가 제약조건으로 표현 안 됨

### 제안 — UI
1. 컬럼 그리드에 **PK명 / FK명 / 참조 테이블 / 참조 컬럼** 입력 셀 추가 (PK_YN/FK_YN 체크 시 활성화)
2. 또는 PK/FK 체크 시 별도 미니 모달로 이름·참조 입력 받기
3. 입력 안 하면 자동 생성 규칙 — `PK_<TABLE>` / `FK_<TABLE>_<PARENT>` 같은 패턴

### 제안 — 백엔드 cascade
- ADD/UPDATE 시 PK_YN='Y' 변경 감지 → tb_data_model_index INSERT (UNIQUE/PK 인덱스) + tb_data_model_constraint INSERT (CONSTRAINT_TYPE='P')
- FK_YN='Y' 변경 감지 → tb_data_model_constraint INSERT (CONSTRAINT_TYPE='R' + REF_OWNER/REF_TABLE_NM/REF_COLUMN_NM)
- PK_YN/FK_YN 해제 시 cascade DELETE 또는 비활성화 정책

### 영향도
- 컬럼 그리드에 셀 4개 (PK명, FK명, 참조 테이블, 참조 컬럼) 추가
- saveAttrs 백엔드에 cascade INSERT/UPDATE 로직 — 5단계
- 매퍼 신규 — `upsertIndexFromPk`, `upsertConstraintFromPk`, `upsertConstraintFromFk`
- DDL 생성기에 PK/FK 제약조건 출력 (이미 일부 있을 수 있음 — 확인)

### 추정
- UI + 백엔드 cascade + 매퍼 — **3~5일**

### 효과
- DataQ 가 ERwin 없이도 모델링 완결성 확보
- 신규 환경 구축 시 DataQ → CREATE TABLE + 제약조건 + 인덱스 SQL 한 파일로 끝

---

## #87-4. 표준화 dryRun 응답에 표준 플래그(termsStndYn / domainStndYn) 미리 평가 🔵

### 사용자 원문 (2026-05-10)
> "변환 성공했는데 왜 이러냐"
> "지금 표준 쪽에 왜 X 로 나오냐 표준 변환 전부 성공한 건데"

### 발견 컨텍스트
- 시연 대본 § 4-3 (모델 ① 한글명 기준 표준화) 실제 테스트 중
- [한글명 기준 표준화] 클릭 → 그리드에 영문명/타입/길이 즉시 채워짐 (dryRun)
- 그러나 표준 컬럼은 X(❌) 그대로 → "변환 성공인데 왜 표준 X?" 사용자 혼란
- [저장] 누른 후 백엔드 동적 검증으로 Y 갱신은 잘 됨 (이번 세션 fix 완료)

### 현재 동작 (이번 세션 fix 후 기준)
- dryRun 응답 `items[]` 에는 `newAttrNm / newAttrNmKr / newDataType / newDataLen / newDataDecimalLen` 만 포함
- 프론트는 이 값들로 row.attrNm 등만 갱신, **termsStndYn / domainStndYn / wordLst 는 _orig 옛값 그대로**
- 그래서 그리드의 표준 컬럼이 X 로 보임 (저장 전)
- [저장] 시점에 백엔드 UPDATE 분기가 동적 표준 검증 (validateAttrStandards + applyStandardFlags) → Y 로 갱신

### 제안
1. **백엔드** `lookupForKr` / `lookupForEng` 안에서 검증을 추가로 돌려 결과를 items 에 포함:
   ```java
   StdDataModelAttrVo recheck = new StdDataModelAttrVo();
   recheck.setAttrNm(newAttrNm); recheck.setDataType(newDataType);
   try {
       validateAttrStandards(recheck);
       applyStandardFlags(recheck);
       item.put("newTermsStndYn", "Y");
       item.put("newDomainStndYn", "Y");
       item.put("newWordLst", recheck.getWordLst());
       item.put("newWordStndLst", recheck.getWordStndLst());
   } catch (Exception e) {
       item.put("newTermsStndYn", "N");
       item.put("newDomainStndYn", "N");
   }
   ```
2. **프론트** items 적용 시 `row.termsStndYn / domainStndYn / wordLst` 도 같이 `$set`

### 효과
- 표준화 직후 그리드의 표준 컬럼이 즉시 ✅ 로 갱신 → 사용자가 결과 확인 후 [저장] 결정
- "왜 표준 X 냐?" 혼란 제거

### 영향도
- 백엔드 lookupForKr / lookupForEng — validateAttrStandards 호출 추가
- 프론트 resolveSelected / resolveByEng — items 적용 시 표준 플래그도 set
- 회귀 영향 없음 (값 추가일 뿐, 기존 흐름 변경 X)

### 추정
- 백엔드 + 프론트 — **반나절**

---

## #87-5. DB 수집 시 스키마 + 객체 종류 단위 선택 수집 🔵

### 사용자 원문 (2026-05-10)
> "DB에서 수집해올 때 스키마별로 테이블·컬럼, 인덱스, 제약조건 각 별로 선택 수집 가능하게.
> 이 기능이 나중에 필요할 것 같아서."

### 발견 컨텍스트
- 시연 대본 § 5-2 (모델 ② 수집 모델) 작성 중
- 현재는 스키마 다중 선택 후 **테이블/컬럼/인덱스/제약조건 일괄 수집**
- 대용량 운영 DB (스키마 수십 개, 테이블 수만 개) 에서 일부 객체만 가져오고 싶은 케이스 대응 부족

### 화면 / 위치
- `데이터 모델 > 데이터 모델 관리 > [신규 모델 + 수집]` 모달
- 백엔드: q-executor `DataModelService.collect()`

### 현재 동작
- 스키마 체크박스 다중 선택 → 선택된 스키마 전체에서 테이블·컬럼·인덱스·제약조건을 모두 수집
- "어느 스키마에서는 테이블만, 다른 스키마에서는 인덱스도" 같은 선택 불가

### 제안 — UI
- 수집 모달에 **스키마 × 객체 종류 매트릭스** 체크박스
  ```
            테이블/컬럼   인덱스   제약조건
  USER1     [✓]          [✓]     [✓]
  HR        [✓]          [ ]     [ ]
  SALES     [ ]          [✓]     [✓]
  ```
- 또는 스키마 선택 후 단계 2: "이 스키마에서 어떤 객체를 가져올지" 체크박스

### 제안 — 백엔드
- `DataModelService.collect()` 에 객체 종류별 ON/OFF 플래그 추가
- 수집 이력 (TB_DATA_MODEL_CLCT) 에 어떤 객체가 수집됐는지 메타 보관

### 영향도
- 수집 모달 UI (스키마 × 객체 매트릭스)
- 수집 서비스 분기 — `if (collectIndex) { collect index... }`
- 수집 이력 메타 — 수집 범위 기록 (`COLLECT_SCOPE_JSON` 같은 신규 컬럼)

### 추정
- UI + 수집 로직 분기 + 이력 메타 — **2~3일**

### 효과
- 대용량 DB 에서 부분 수집 가능 → 모델 가벼움 유지
- 인덱스/제약조건은 운영 부담이 큰 객체이니, 필요한 스키마만 선택해 비용 줄임

---

## #87-6. 진단 결과에서 데이터 모델 직접 수정 / 자동 반영 🔵

### 사용자 원문 (2026-05-10)
> "진단 결과에서 데이터 모델을 직접 수정 가능할 수 있는 케이스가 있음 (표준화 칩, 코멘트 다름, 타입 다름 등등).
> 지금은 일일이 수정하거나 물리 DB 연결 같은 경우 물리 DB 를 재수집하면 그만이겠지만,
> 논리 모델 같은 경우도 그냥 사실 표준화 변환을 다 하는 식으로 작업을 하는 게 맞겠지.
> 그러나 ERwin 임포트 같은 경우는 모델을 먼저 수정하고 DDL 로 내보낼 수 있어야 함.
> 이에 표준 진단에서 직접 모델을 자동 변경할 수 있는 방법 또한 넣어 놔야 함."

### 발견 컨텍스트
- 시연 대본 § 5-5 (모델 ② 표준 진단 결과 분석) + § 6 (모델 ③ XMI 임포트) 검토 중
- 현재 진단 결과는 "이슈 보고" 만 함 — 사용자가 직접 컬럼 화면으로 가서 수정해야 함
- 또는 [용어 빠른 등록] 으로 이슈 컬럼 1건씩 표준 사전에 추가 가능
- 그러나 **진단 결과 → 모델 자동 반영** 이 없어 ERwin 임포트 케이스 같은 경우 개선 사이클이 길어짐

### 모델 출처별 시나리오 분기

| 모델 출처 | 이슈 발견 시 권장 흐름 | DataQ 가 자동화해야 할 부분 |
|---|---|---|
| **물리 DB 연결 (수집)** | DB 자체를 ALTER + 재수집 | 재수집 트리거. 모델 자동 수정은 부적절 (실 DB 진실) |
| **논리 모델 (직접 입력)** | 한글명 또는 영문명 기준 표준화 dryRun → 일괄 저장 | 컬럼 화면의 표준화 버튼 (이미 구현) |
| **ERwin / XMI 임포트** | 모델을 표준에 맞게 자동 수정 → DDL 다운로드 → ERwin 으로 다시 import | **진단 결과 화면에서 직접 모델 자동 반영 기능 필요** |

### 제안 — 진단 결과 화면의 [모델 자동 반영] 액션
- 진단 결과 테이블 상세 / 컬럼 상세 행에 [모델 반영] 버튼 추가
- 클릭 시 — 해당 이슈 유형별로 표준 권장값을 모델에 적용
  - **TERM_NOT_EXIST** (용어 미존재) — [용어 빠른 등록] 모달 (현재 있음) → 등록 후 모델 표준 플래그 갱신
  - **TERM_KR_NM_MISMATCH** (한글명 불일치) — 한글명을 표준값으로 일괄 변경
  - **DATA_TYPE_MISMATCH** (타입 불일치) — 타입을 표준값으로 일괄 변경
  - **DATA_LEN_MISMATCH** (길이 불일치) — 길이를 표준값으로 일괄 변경
- 다중 선택 + 일괄 [선택 항목 모두 표준값 반영] 액션
- 반영 후 자동 [재진단] 또는 진단 결과 화면 캐시 갱신
- 반영 결과 confirm — 변경 전/후 비교 + 사용자 확인 후 commit

### 모델 출처 인식 — 권장 흐름 분기
- 모델의 데이터소스 (TB_DATA_MODEL.DM_DS_ID) 가 NULL — 논리 모델 / ERwin 임포트 → [모델 반영] 활성
- 데이터소스 있음 + 수집 이력 있음 — 물리 DB 연결 → [모델 반영] 비활성 + "DB 변경 후 재수집" 안내

### 영향도
- 진단 결과 화면 (DSDataDiagResult.vue) — 행별 [모델 반영] 액션 + 일괄 액션
- 백엔드 신규 endpoint — `/api/diag/applyToModel` (이슈 유형별 분기 처리)
- 매퍼 신규 — 한글명 / 타입 / 길이 일괄 update (기존 saveAttrs 활용 가능)
- 모델 출처 판단 로직 — DM_DS_ID 기반

### 추정
- UI + 백엔드 + 매퍼 — **3~5일**

### 효과
- ERwin 임포트 케이스에서 표준화 사이클 단축 — 진단 → 자동 반영 → DDL 다운로드 → 다시 ERwin 으로 import (왕복 1번)
- 논리 모델도 진단 결과를 보고 즉시 보정 가능
- 물리 DB 케이스는 잘못 자동 반영되지 않게 안전 분기

---

## #87-7. 용어 **수정** 모달 UI 단일폼 통일 🔵 (#87-8 다음 진행)

### 사용자 원문 (2026-05-11)
> "여기에 용어 수정이랑 코드 관련 작업을 넣어야 할듯"

### 우선순위
**#87-8 (코드 등록 모달) 가 선행** — 사용자 결정 (2026-05-11). #87-8 마무리 후 본 항목 착수.

### 발견 컨텍스트
- 81/82 번에서 용어 **등록** 모달은 단일폼 + 디바운스 자동 분석 (`analyzeTermsBatch`) + 코드 picker + 메타 접기로 개선됨
- 그러나 **수정** 모달 (`updateTermModalShow`) 은 옛 **3단계 스테퍼** 구조 그대로 — 사용자가 등록·수정을 오갈 때 UX 비대칭
- 후속 보강 필요 — 정확한 우선순위·시연 시 발견 케이스 별도 노트

### 화면 / 위치
- `데이터 표준 사전 > 용어 > [수정]` 모달
- 컴포넌트: `q-center/vue/front/src/components/DSTerm.vue` (L477~ `updateTermModalShow`)
- 기존 등록 모달 1차 구현: 커밋 `858f091`, v2: 커밋 `c0baba7`

### 현재 동작
- 3단계 스테퍼: 용어명 입력 → 단어 목록 선택 → 용어 정보 입력 (도메인·메타)
- 자동 분석 트리거 없음 — 사용자가 [다음] 단계 진행해야 단어 조회
- 코드 도메인은 v-autocomplete 단순 (picker 다이얼로그 X)
- 메타 영역 항상 펼침

### 제안 — 등록 모달과 동일 패턴 적용
1. v-stepper 제거 → 단일 폼
2. 한글 용어명 입력 1초 디바운스 후 `analyzeTermsBatch` 자동 호출
3. 코드 도메인 선택 시 picker 다이얼로그 (등록 모달과 동일 컴포넌트 재사용)
4. 메타 (이음동의어 / 시스템CD / 제정차수 / 공통표준) v-expansion-panel default 접힘
5. 기존 수정 흐름 (행 선택 → 모달 진입 시 값 채우기) 보존 — 분석 결과와 기존 값 동기화 정책 필요

### 정책 결정 포인트
- 수정 모달 진입 시 기존 분리된 단어 set 우선 표시 vs 즉시 재분석
- 사용자가 용어명을 바꾸면 자동 재분석 트리거 — 등록과 동일
- 영문약어 readonly 정책 — 등록은 자동 합성, 수정은 원본 보존? 합성 갱신?

### 영향도
- DSTerm.vue 의 updateTermModalShow 영역 한정 (등록 영역과 코드 picker 컴포넌트 공유 가능)
- 백엔드 API 변경 없음 (`analyzeTermsBatch` / `updateTerm` 기존 그대로)
- 수정 결과 검증 — 셀레니움 [test_term_register_v2.py](테스트/selenium/test_term_register_v2.py) 패턴 차용해 update 시나리오 신규

### 추정
- 등록 모달 패턴 복제 + 기존 값 매핑 — **반나절~1일**

---

## #87-8. 코드 등록/관리 화면 UX 정비 🔵 **(선행 1순위)**

### 사용자 원문 (2026-05-11)
> "여기에 용어 수정이랑 **코드 관련 작업**을 넣어야 할듯"
>
> "지금 코드에서 따로 등록하지 않고 용어만 등록했을때 코드에서 나오는 항목은 코드 그룹이 지정된 것만 보여주나?"

### 우선순위
**#87-7 (용어 수정 모달) 보다 선행** — 사용자 결정 (2026-05-11). 용어 등록 picker 의 코드 source 정책이 정리돼야 수정 모달도 일관 적용.

### 발견 컨텍스트
- 81/82 작업으로 용어 등록 모달에 **코드 picker 다이얼로그** 가 도입됨
- 사용자 질문 검증 결과 — **picker source 는 `TB_TERMS` 에서 `CODE_GRP IS NOT NULL` 인 row 만** (`selectCodeInfoList`, [terms.xml:391-428](../q-common/src/main/resources/mapper/stnd/terms.xml#L391-L428))
- 즉 **별도 코드 사전 (`TB_CODE`) 의 row 는 picker 에 안 나옴** — DSCode 에서 따로 등록한 코드는 용어 등록 시 활용 불가
- 86번 #35 (단어/용어 사전 엑셀 드롭다운 UX 통일) 흐름의 연장선

### 핵심 정책 결정 사항
**Q.** picker 의 source 를 어떻게 가져갈지?

| 안 | 동작 | 장단점 |
|---|---|---|
| (a) **TB_TERMS.CODE_GRP** 만 유지 (현행) | 용어로 먼저 등록 후 picker 노출 | 사전 단일 진실. 코드 사전 별도 운영 의미 약화 |
| (b) **TB_CODE 만** picker source | 코드 사전을 정식 source 로 | DSCode 의 row 가 picker 에 보임. 용어와 분리 |
| (c) **TB_CODE ∪ TB_TERMS.CODE_GRP** 합집합 | 양쪽 다 picker 에 노출 (중복 정리 필요) | 호환성 ↑, 중복 키 정책 필요 |

→ 정책 결정 후 매퍼 / picker / DSCode 정비 방향 확정.

### 화면 / 위치
- `데이터 표준 사전 > 코드` (`DSCode.vue`, 약 3040줄)
- 관련 화면: 코드 항목 (소코드) 관리 모달
- 용어 등록의 코드 picker (DSTerm.vue 내부 v-dialog) 와 source 정책 공유

### 현재 동작
- DSCode.vue — 별도 CRUD 화면 (코드 + 소코드 별도 모달)
- 용어 등록 picker = TB_TERMS.CODE_GRP IS NOT NULL 만 (위 발견)
- 도메인 / 데이터타입 자동 추정 없음
- 단어 분리·자동 ABRV 합성 같은 자동화 부재 (코드명·코드그룹 모두 사용자 수동 입력)

### 제안 — 정비 후보 (정책 결정 후 단계별)
1. **picker source 정책 확정** (위 3안 중 택일) + 매퍼 / DSCode 정비
2. **단일 폼 + 자동 분석** — 코드명 입력 시 단어 분리 + 코드그룹 자동 추정 (용어 등록과 동일 패턴)
3. **엑셀 드롭다운 UX** — 도메인 사전과 동일 (#86-#35 패턴)
4. **데이터타입 17종 표준** — #87-2 의 표준 타입과 통일
5. **소코드 일괄 입력** — 엑셀 붙여넣기 / 인라인 그리드 입력
6. **시드 코드 정책** — 행안부 표준 코드 시드 (TB_QUAL_RULE_CATALOG 의 SEED_* 패턴 참고)

### 영향도
- DSCode.vue 전체 리뉴얼 (대형 변경)
- 코드 INSERT/UPDATE API 의 자동 추정 분기 추가
- 매퍼: `selectCodeInfoList` source 정책 적용, 코드명 단어 분리 → 코드그룹 자동 매칭
- 용어 등록의 코드 picker 와 source 일관

### 추정
- 정책 확정 후 — 1차 source 정비 **반나절**, 단일폼 + 자동 분석 **1~2일**, 소코드/시드 **추가 1~2일**

### 진행 순서 (제안)
1. **단계 1 (선행 1순위)** — picker source 정책 확정 + 매퍼 정비 (반나절)
2. **단계 2** — DSCode 등록 모달 단일폼 + 자동 분석
3. **단계 3** — 소코드 일괄 입력 / 시드

---

## #87-9. 용어 변경 상세 모달 — wordList 이하 raw 객체 노출 차단 🔵

### 사용자 원문 (2026-05-11)
> "용어 이력에 단어 uuid 노출"
>
> "용어 이력에 wordList 부터는 보여줄 필요 없는데"
>
> "wordList 이하 숨김  이스샷 보면 아래부터 쓸모 없어"

### 발견 컨텍스트 (스크린샷 검증 완료)
- 화면 = **변경 상세 모달** (변경유형 / 대상 / 변경자 / 변경일시 / 요약 + "등록된 정보" 항목·값 표)
- 예시: 용어 "나래자료제목" 등록 시 변경 상세를 열면
  - 의미 있는 항목 (용어명, 영문약어, 도메인명, 데이터타입/길이, 코드그룹, 담당기관, 공통표준, 제정차수, 승인상태 등) 까지는 OK
  - 그 뒤 `wordList` row 에 **`[StdTermsVo.Word(termsId=1Aa*lpj2QQE8cn_hpRqcFW...` 같은 Java/Vue 객체 toString() 결과**가 그대로 노출
  - 이어서 `wordId` / `단어명` / `wordOrd` × 3 단어 분량이 raw 펼쳐짐 (사용자에게는 잡음)

### 화면 / 위치
- 변경 상세 모달 (변경이력 그리드에서 행 클릭 시 열림)
- 백엔드: `TB_APRV_STATS` 또는 별도 변경이력 매퍼 → JSON 응답을 그대로 항목·값 표로 펼침
- 프론트: 변경 상세 표 render 시 응답 dict 의 모든 key 를 row 로 노출

### 현재 동작
- 백엔드 응답에 `wordList = List<StdTermsVo.Word>` 같은 객체 배열이 포함됨
- 프론트가 각 key 를 row 로 풀 때 객체의 `toString()` 이 그대로 보임 → `StdTermsVo.Word(termsId=..., wordId=..., wordNm=..., wordOrd=...)` 형식
- 이어서 객체의 각 필드 (wordId, 단어명, wordOrd) 도 단위 row 로 펼쳐져 사용자 화면이 raw 잡음으로 채워짐

### 제안 (단순 + 안전)
1. **변경 상세 표 화이트리스트** — 사용자에게 의미 있는 key 만 노출
   - 노출: 용어명 / 영문약어 / 용어설명 / 도메인명 / 데이터타입 / 데이터길이 / 소수점길이 / 허용값 / 저장형식 / 표현형식 / 코드그룹 / 담당기관 / 이음동의어 / 공통표준 / 제정차수 / 승인상태
   - 숨김: `wordList`, `wordId`, `wordOrd`, `partOfSpeech`, `wordStndLst`, 기타 내부 raw 필드
2. **또는 블랙리스트** — `wordList` 부터 이하 raw 필드 키만 명시 제외 (구현 짧음)
3. **단어 구성을 남기고 싶다면 별도 영역** — "구성 단어: 나래 / 자료 / 제목" 같이 단어명만 join 으로 한 줄 표시 (UUID 숨김)

### 영향도
- 변경 상세 모달 컴포넌트 (key list 필터링 한 줄 추가)
- 또는 백엔드 응답 시점에 raw List<Word> 객체를 직렬화 전에 제거 / 단어명 배열로 치환
- 매퍼 수정 최소 — 응답 시 wordList 제외 또는 단어명 배열로 변환

### 추정
- **30분 ~ 1시간** (프론트 표 필터링 한 줄 또는 백엔드 직렬화 분기)

---

## #87-10. 컬럼 → 테이블 진입 시 데이터모델명 자동 매핑 누락 케이스 🔵

### 사용자 원문 (2026-05-11)
> "컬럼 -> 테이블에서 타고 들어갈때 데이터 모델명 자동매핑 체크 , 안될때가 있었음"

### 발견 컨텍스트
- `데이터 모델 > 컬럼` 화면에서 특정 컬럼 행을 클릭 → `테이블` 화면 (해당 컬럼이 속한 테이블) 으로 이동하는 흐름
- 이동 시 모델 드롭다운에 **현재 컬럼이 속한 데이터모델이 자동 선택**되어야 하는데 가끔 빈 상태로 진입
- 재현 시점·조건 미확정 — 후속 정확화 필요

### 화면 / 위치
- `데이터 모델 > 컬럼` → 행 클릭 → `데이터 모델 > 테이블`
- 컴포넌트: `DSDatamodelStatusColumn.vue` → `DSDatamodelStatusTable.vue`
- eventBus 패턴 (`pendingXxx` + `openXxx` emit / activated hook) 사용 추정

### 현재 동작 (추정)
- 컬럼 행 클릭 → eventBus.pendingTable = { dmId, objNm } emit
- 테이블 화면 activated hook 에서 pending 받아 모델·테이블 자동 선택
- **타이밍 / 모델 목록 로드 순서** 에 따라 dmId 가 list 에 들어가기 전에 set 되면 매핑 실패 가능

### 제안 — 디버깅 + 안정화
1. 재현 케이스 수집 (어떤 컬럼/모델에서 발생하는지 노트)
2. eventBus pending 받은 후 모델 list `await` 보장 — list 로드 완료 후 selected 설정
3. fallback — pending.dmId 가 list 에 없으면 추가 fetch 후 강제 선택
4. 실패 시 swal/toast 로 사용자에게 알림 (silent fail 방지)

### 영향도
- DSDatamodelStatusTable.vue (또는 동등 컴포넌트) activated hook 수정
- 데이터모델 list 로드 시점 보장 (Promise 또는 watcher)
- 회귀 없음 — 기존 경로는 그대로, 매핑 실패 케이스만 보강

### 추정
- 재현 확정 후 **반나절** (재현 못 하면 +1일)

### 후속
- 같은 패턴이 다른 화면 이동에도 있는지 (예: 진단 결과 → 컬럼) 일괄 점검 권장

---

## #87-11. 승인 프로세스 — 용어 반려 시 포함 단어 처리 정책 🔵

### 사용자 원문 (2026-05-11)
> "승인 프로세스 -> 용어에 포함된 단어를 반려할때 어떻게 하는지"

### 발견 컨텍스트
- 용어는 단어들의 조합으로 등록됨 — 일부는 기존 표준 단어, 일부는 용어 등록 시 같이 신규 등록된 단어
- 용어 승인/반려 시 **포함된 단어 (특히 신규 단어) 의 처리 정책이 명확하지 않음**
- 기존 cascade 반려 정책 (단어 반려 시 연관 미승인 용어 경고) 은 정착되어 있으나 **역방향 (용어 반려 → 신규 단어) 미정의**

### 화면 / 위치
- `관리 > 승인` 화면 (`MMApproval.vue` 추정)
- 백엔드: `TB_APRV_STATS` UPDATE 분기 + `TB_TERMS` / `TB_WORD` 의 APRV_YN cascade

### 현재 동작 (추정)
- 용어 반려 시 — `TB_TERMS.APRV_YN='N'` 또는 row 삭제
- 그 용어에 포함된 신규 단어 (역시 미승인 상태) 는 **그대로 잔류** — 다른 용어가 같은 단어를 사용 중인지 검사 없음
- 또는 같이 cascade 반려 — 정책 어느쪽인지 코드 직접 확인 필요

### 정책 후보
| 안 | 동작 | 장단점 |
|---|---|---|
| (a) **단어는 그대로 보존** | 용어 반려 시 단어는 미승인 상태 유지 → 다른 용어가 살릴 수 있음 | 안전. 단 잔재 데이터 |
| (b) **같이 cascade 반려** | 단어도 함께 반려/삭제 (단, 다른 용어가 사용 중이면 보존) | 깔끔. 종속성 검사 필요 |
| (c) **반려 사유에 따라 분기** | 사용자가 "단어도 같이 반려" 체크박스 선택 | 유연. UI 복잡 |

### 제안
- 정책 확정 + 승인 화면 UX 보강 — 반려 시점에 "이 용어에 포함된 신규 단어 N개 / 다른 용어가 사용 중 M개" 미리 보여주기

### 영향도
- 승인 매퍼 — 반려 시 단어 종속성 검사 + cascade 처리 SQL
- 승인 화면 — 반려 확인 모달에 영향 단어 카운트 노출
- 셀레니움 — 반려 cascade 시나리오 신규

### 추정
- 정책 결정 후 — **1일** (검사 + cascade + UI)

### 후속 — 본 항목은 #87-12 (알림/액션 정비) 와 연동
- 반려 결과 알림에 cascade 영향 (단어 N개 함께 처리) 같이 표시 권장

---

## #87-12. 알림 ↔ 액션 통합 (지금 따로 노는 느낌) 🔵

### 사용자 원문 (2026-05-11)
> "지금 알림이랑 액션이 살짝 따로 노는 느낌임"

### 발견 컨텍스트
- 알림 (toast / swal / 상단 알림 영역) 과 그에 연결된 액션 (재요청 / 이동 / 취소 등) 의 흐름이 **분리** 되어 있어 사용자가 알림 본 뒤 별도 화면을 찾아가야 함
- 예시 (추정):
  - 승인 결과 알림 → 해당 용어/단어로 이동 액션 없음
  - 진단 완료 알림 → 결과 화면 바로가기 없음
  - 수집 실패 알림 → 재시도 / 로그 보기 액션 분리
- 후속 보강 — 사용자가 본 구체 케이스 (어느 알림에서 어느 액션이 빠졌는지) 별도 노트 필요

### 화면 / 위치
- 전역 알림 시스템 (swal / 상단 toast / 알림 센터)
- 관련: `MMApproval.vue` 승인 결과, `DSDataDiag.vue` 진단 알림, `DSDatamodelCollection.vue` 수집 알림 등

### 제안 — 알림 패턴 통일
1. **모든 알림에 액션 버튼 의무화** — `[보기]` / `[재시도]` / `[취소]` 등 최소 1개
2. **알림 센터 (벨 아이콘)** — 누적된 알림을 일관 인터페이스에서 확인 + 클릭 시 해당 화면으로 라우팅
3. **알림 메타 표준화** — `{ type, message, action?: { label, target, payload } }` 구조 통일
4. **eventBus 라우팅 정책** — 알림 액션 클릭 시 eventBus.openXxx + pendingXxx 패턴 일관 적용

### 영향도
- 전역 알림 컴포넌트 또는 utility 신설 (`$nd.notify({...})`)
- 기존 swal / toast 호출처 일괄 마이그레이션 (대형)
- 알림 센터 UI 신규 (선택)

### 추정
- 패턴 정의 + 기본 컴포넌트 — **2~3일**
- 호출처 마이그레이션 — **추가 2~3일** (점진 가능)

### 진행 순서 (제안)
1. **단계 1** — 알림 메타 구조 표준화 + 신규 utility
2. **단계 2** — 핵심 알림 (승인/진단/수집) 부터 액션 추가
3. **단계 3** — 알림 센터 (선택)

---

## #87-13. 진단 결과 — 유사 용어 추천 + 즉시 모델 반영 🔵

### 사용자 원문 (2026-05-12)
> "표준진단에서 결과를 가지고
> 용어가 없거나 했을때 , 유사용어 (현재 영문명 기준, 단어 기준, 데이터 타입 기준으로)
> 추천 해서 변경할 수 있도록 하는 기능 (아직 추가 안된 데이터 모델 즉각 변경 기능을 사용)"

### 발견 컨텍스트
- 표준화 진단 결과에서 **용어 미존재 (TERM_NOT_EXIST)** 이슈가 가장 많음
- 현재는 [용어 빠른 등록] 으로 신규 등록만 가능 — 이미 있는 비슷한 용어로 매핑하는 흐름 없음
- "회원ID" 가 표준에 없을 때 영문명/단어/데이터타입이 유사한 "사용자ID" 같은 기존 용어를 추천해 모델을 즉시 변경할 수 있어야 함
- **#87-6 (진단 결과 → 모델 자동 반영)** 의 즉시 변경 기능을 source 로 활용

### 화면 / 위치
- `데이터 품질 진단 > 표준화 진단 결과` 상세 (DSDataDiagResult.vue)
- 이슈 유형이 TERM_NOT_EXIST 인 row 에 [유사 용어 추천] 액션
- 백엔드: 신규 endpoint `/api/diag/suggestSimilarTerms` (유사도 산출)

### 현재 동작
- TERM_NOT_EXIST 이슈 → [용어 빠른 등록] (신규 등록) 만 노출
- 이미 있는 유사 용어와의 매핑 흐름 없음

### 제안 — 유사도 산출 축
1. **영문명 기준** — Levenshtein / 토큰 단위 유사도 (예: `USER_ID` ↔ `MBR_ID` 비교)
2. **단어 기준** — 용어 구성 단어의 교집합 비율 (예: ["회원", "식별자"] ↔ ["사용자", "식별자"] = 50%)
3. **데이터 타입 기준** — 같은 도메인 또는 같은 데이터 타입/길이 (보조 필터)
4. **한글명 유사도** (추가 후보) — n-gram 또는 자모 분해 기반

### 제안 — UI 흐름
1. 진단 결과 row 의 [유사 용어 추천] 클릭 → 미니 모달
2. 모달에 후보 용어 N개 (유사도 점수 + 축 표시 — 영문/단어/타입 어느 축이 매치됐는지 배지)
3. 사용자가 후보 선택 → [이 용어로 변경] 버튼
4. 백엔드 cascade — 해당 컬럼의 ATTR_NM / ATTR_NM_KR / DATA_TYPE / DATA_LEN 을 선택 용어 표준값으로 갱신 (#87-6 의 적용 endpoint 활용)
5. 적용 후 즉시 재진단 또는 캐시 갱신

### 영향도
- 진단 결과 컴포넌트에 [유사 용어 추천] 액션 + 모달 추가
- 백엔드 신규 endpoint — 유사도 산출 (영문명 distance + 단어 교집합 + 타입 매칭)
- 매퍼 신규 — `selectSimilarTerms(engNm, words[], dataType)` (TB_TERMS 전체 스캔 또는 인덱스)
- #87-6 의 모델 즉시 반영 기능 의존 — 본 항목은 #87-6 완료 후 착수 권장

### 추정
- 유사도 산출 백엔드 + 추천 모달 UI + #87-6 연동 — **2~3일** (단, #87-6 선행 필요)

### 효과
- 비슷한 표준 용어가 이미 있을 때 신규 등록 대신 재사용 유도 → 사전 비대화 방지
- ERwin 임포트 / 논리 모델에서 진단 → 추천 → 즉시 반영 사이클로 모델 정비 속도 ↑
- 표준 사전 운영 정책 (#87-1 동의어 정책) 과 연동 가능

### 의존 관계
- **선행 필요**: #87-6 (진단 결과 → 모델 자동 반영) — 본 항목은 #87-6 의 모델 변경 endpoint 를 source 로 활용
- **연관**: #87-1 (동의어 정책) — 유사 용어 발견 시 동의어로 등록할지 / 그대로 매핑할지 정책

---

## 누적 발견 패턴 (메타 회고)

### 1. 운영 DB 대규모 환경의 UX 부족
- #87-2 도메인 데이터 타입 분산 — DBMS raw 타입이 사전을 오염
- #87-5 수집 단위 선택 — 부분 수집 불가
- (관련) 진단 제외 관리 (cascade 적용 완료) 도 같은 카테고리

### 2. 모델링 완결성
- #87-3 PK/FK cascade — 인덱스/제약조건 자동 생성. 현재 DataQ 는 ERwin 없이도 표준화·진단까지 OK. 다음 단계로 모델링 완결.
- (관련) 컬럼 영문명/테이블 cascade rename (이번 세션 fix 완료) 도 같은 카테고리

### 3. dryRun UX 정밀화
- #87-4 표준화 결과 즉시 표시 — 저장 직전 상태 정보 정확성
- (관련) 표준화 dryRun 정책 자체는 이번 세션 도입. 이를 한층 매끄럽게 하는 follow-up

### 4. 표준 사전 운영 정책
- #87-1 동의어 vs 기존 용어 — 입력값 모호성 처리
- #87-7 용어 수정 모달 단일폼 통일 — 등록/수정 UX 비대칭 해소
- #87-8 코드 picker source 정책 + DSCode UI 정비 (**선행 1순위**)
- #87-11 용어 반려 시 포함 단어 cascade 정책 — 역방향 종속성
- (관련) 단어 선승인 / cascade 반려 정책은 정착됨. 다음 단계로 동의어·코드 source·반려 cascade 영역.

### 5. 진단 → 모델 보정 사이클 단축
- #87-6 진단 결과에서 모델 자동 반영 — ERwin 임포트 케이스에서 특히 중요
- #87-13 유사 용어 추천 + 즉시 반영 — 영문명/단어/타입 축 기반, #87-6 의존
- (관련) 모델 출처별 분기 (논리 / 수집 / 임포트) 흐름 정리

### 6. 이력 / 감사 가독성
- #87-9 용어 이력에 단어 UUID 노출 — JOIN 으로 한글명 같이 보이게
- (관련) 이력/감사 화면의 raw 식별자 노출 일괄 점검 권장

### 7. 화면 간 이동 안정성
- #87-10 컬럼 → 테이블 자동 매핑 누락 — eventBus pending + 모델 list 로드 타이밍
- (관련) 같은 패턴이 다른 화면 (진단 결과 → 컬럼 등) 에도 있는지 일괄 점검

### 8. 알림 ↔ 액션 통합
- #87-12 알림이랑 액션이 따로 노는 느낌 — 알림 메타 표준화 + 액션 버튼 의무화 + 알림 센터
- (관련) 승인/진단/수집/반려 결과 → 후속 액션 일관 라우팅 (#87-11 의 반려 결과 알림과 연동)

---

## 운영 절차

- 새 항목 발견 시 → 본 문서 끝에 `#87-N` 으로 추가 (사용자 원문 보존 필수)
- 작업 시작 시 상태를 🟡 진행중 + 작업 PR/커밋 링크 추가
- 완료 시 ✅ + 완료 일자 + 검증 방법 (수동/Selenium 케이스명)
- 보류 결정 시 🟢 + 보류 사유 + 재검토 일정
