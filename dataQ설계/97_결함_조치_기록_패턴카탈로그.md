# 97. 결함 조치 기록 · 패턴 카탈로그

**용도 2가지**
1. **조치 기록** — 발견한 결함의 증상·원인·조치·검증을 남긴다
2. **패턴 카탈로그** — 같은 유형을 **다시 찾을 수 있게** 탐색 명령과 예방책을 남긴다

> 새 결함을 만나면 먼저 §2 패턴 목록에서 유사 패턴을 찾아라. 대부분 이미 겪은 유형이다.
> 새 유형이면 §2 에 패턴을 추가하고 **탐색 명령을 반드시 같이 적어라.** 그게 이 문서의 값이다.

**갱신 규칙**: 결함 1건 = §3 에 1행 + 상세 1절. 새 패턴이면 §2 에도 1절.

---

## 1. 현황 요약

| 상태 | 건수 |
|---|---|
| 수정 완료 + 런타임 검증 | **7** |
| **실증 확인** (재현 성공, 미조치) | **10** |
| 결함 아님 (검증으로 반증) | 1 |
| 스키마 한계 (구조 변경 없이는 해결 불가) | 1 |
| 실증 미완 (코드 근거만) | 1 |
| 관찰 필요 (사양 확인 후 판단) | 5 |

> **반증 1건이 중요하다.** `DEF-08a` 는 소스 추적 단계에서 "OBJ_OWNER 누락 = 결함" 으로 분류됐으나,
> 실측 결과 SET 서브쿼리가 상관 결합돼 있어 결과가 정확했다.
> **소스 추적 결과는 가설이고, 실증 전까지 결함이 아니다.**

### 실증 결과 요약 (2026-08-23)

| 결함 | 재현 증거 |
|---|---|
| DEF-02 | 테이블 삭제 후 — OBJ=0 ATTR=0 인데 **고아 IDX=1 CON=1 inboundFK=1 FK_PARENT=1** |
| DEF-03 | rename 200 성공 후 — 자식의 `fk_parent_obj_nm` 이 **옛 이름으로 1건 잔존** |
| DEF-04 | 일반사용자 UPDATE — 이력 `MODIFY_ATTR/TIER1/DRAFT` 인데 모델은 `VARCHAR(10)→VARCHAR(99) APPROVED` |
| DEF-05 | 일반사용자 DELETE — 삭제전 1 / 삭제후 0 / **반려후 0** (복구 안 됨) |
| DEF-06 | 부모 반려 → `P1=REJECTED C1G=REJECTED` 인데 **손자 `G1=SUBMITTED`** |
| DEF-07 | 자식 C1G — **모델=REJECTED / 이력=SUBMITTED** (불일치) |
| DEF-08c | A 소유 테이블 반려 → **B 소유 컬럼이 SUBMITTED→REJECTED** |
| DEF-09 | `APPROVED` 인 건에 reject 호출 → `resultCode=200`, **APPROVED→REJECTED** |
| DEF-10 | 진단 후 — DRAFT 컬럼이 **진단결과 0건인데 flag `Y/Y` 로 승격** |
| DEF-12 | 미승인 용어 약어를 쓴 컬럼의 **`TERM_NOT_EXIST` 0건** (미승인인데 '있는 용어'로 취급) |

발견 경로별 분포 — **경로마다 잡히는 결함이 다르다**:

| 발견 경로 | 건수 | 이 경로에서만 잡히는 것 |
|---|---|---|
| 정적 대조 (호출 ↔ 정의) | 1 | 존재하지 않는 statement 호출 |
| UI 테스트 실패 추적 | 2 | 화면이 실제로 부르는 경로의 상태 오류 |
| **API 일괄 실호출** | 1 | **화면이 안 부르는 경로** — UI 테스트 전부 통과해도 안 잡힘 |
| **소스 상태전이·cascade 추적** | 12 | **아직 아무도 실행하지 않은 조합** |
| 통합 시나리오 실측 | 1 | 영역 간 인과 |

---

## 2. 패턴 카탈로그

### P1. 스키마·계약 변경 잔재 — 사라진 대상을 계속 참조

**정의**: 컬럼·테이블·statement 가 제거·개명됐는데 참조하는 쪽이 안 따라간 경우. 런타임에만 터진다.

**사례**
- 결함① — `terms.selectTermsInfo` 호출, 실제 정의는 `selectTermsInfoByNm` 뿐
- 결함④ — `impact.xml` 이 `a.DM_CLCT_ID` 참조, CLCT 폐기 때 컬럼 제거됨

**왜 안 잡혔나**: 컴파일이 안 잡는다(MyBatis 는 문자열). 화면이 그 버튼을 안 누르면 UI 테스트도 안 잡는다.

**탐색 명령**
```bash
# (1) Java 가 부르는 statement ↔ XML 정의 대조
grep -ohE '<(select|insert|update|delete)[ \t][^>]*id="[^"]+"' q-common/src/main/resources/mapper/stnd/*.xml \
  | grep -oE 'id="[^"]+"' | sed 's/id="//;s/"//' | sort -u > /tmp/defined.txt
NS=$(grep -m1 -ohE 'namespace="[^"]+"' q-common/src/main/resources/mapper/stnd/*.xml | sed 's/namespace="//;s/"//' | paste -sd'|')
grep -rhoE "\"($NS)\.[a-zA-Z0-9_]+\"" --include="*.java" q-center/src q-executor/src | tr -d '"' | sed 's/.*\.//' | sort -u > /tmp/called.txt
comm -23 /tmp/called.txt /tmp/defined.txt   # 호출하는데 정의 없음

# (2) 매퍼가 참조하는 컬럼 ↔ 실제 스키마 대조 (INSERT 절 기준이 가장 정확)
#     94번 §3-4 의 방식. UPDATE SET 파싱은 #{x,jdbcType=} 오인으로 신뢰 불가

# (3) 매퍼 참조 테이블 ↔ DDL  (대소문자 무시 필수)
grep -ohiE '\btb_[a-z0-9_]+' q-common/src/main/resources/mapper/stnd/*.xml | tr 'A-Z' 'a-z' | sort -u
```

**예방**: 컬럼·테이블 제거 시 `grep -ri '<제거대상>' q-common/src/main/resources/mapper/` 를 **반드시** 돌린다. CLAUDE.md 의 DDL self-check 절차가 이걸 위한 것.

---

### P2. 영향 행 수 미검사 — 0건 처리인데 성공 응답

**정의**: `UPDATE`/`DELETE` 가 0행을 처리해도 반환값을 안 보고 200 을 돌려주는 경우. **조용히 실패**한다.

**사례**
- 결함② — `updateAttr`/`updateObj`/`deleteAttr`. PK 에 `OBJ_OWNER` 가 들어가는데 클라이언트가 안 보내면 0건인데 200

**왜 위험한가**: 사용자는 저장됐다고 믿는다. 다음 조회에서 옛 값이 보여도 캐시 문제로 오해한다.

**탐색 명령**
```bash
# 반환값을 버리는 update/delete 호출
grep -rnE '^\s*(sqlSessionTemplate|session)\.(update|delete)\(' --include="*.java" q-center/src q-executor/src \
  | grep -vE '^\S+:[0-9]+:\s*(int|long|Integer)\s' 
```

**판정**: 반환값을 안 받는 게 전부 결함은 아니다. **키가 복합 PK 이고 클라이언트 입력에 의존**하면 결함이다.

**대조 사례 (올바른 구현)**: `DiagTargetController` 는 `int cnt = ...update(...)` 후 `res.put("success", cnt > 0)` 로 정직하게 돌려준다. 같은 프로젝트 안에 좋은 예가 있으니 그 패턴을 따를 것.

**예방**: 복합 PK 대상 UPDATE/DELETE 는 **영향 행 수 검사 + 대상 식별자를 담은 메시지** 를 기본으로 한다.

---

### P3. 다중 완료 경로 중 일부만 후속 처리

**정의**: 같은 작업을 끝내는 경로가 여러 개인데, 후속 상태 갱신은 일부 경로에만 있는 경우.

**사례**
- 결함③ — 스케줄 로그를 마감하는 경로가 3곳. `markFinish` 만 `LAST_EXEC_*` 를 갱신하고 ERROR 마감 2곳은 안 함 → 실패한 스케줄은 "마지막 실행" 이 영원히 빈칸

**탐색 명령**
```bash
# 같은 매퍼를 부르는 지점이 여러 개인지 → 각 지점의 후속 처리가 같은지 비교
grep -rn '"diagSchedule.updateLogFinish"' --include="*.java" q-center/src q-executor/src
grep -rn '"diagSchedule.updateScheduleLastExec"' --include="*.java" q-center/src q-executor/src
# 호출 지점 수가 다르면 의심
```

일반화:
```bash
# 매퍼 statement 별 Java 호출 지점 수 — 1보다 크면 경로 분기 존재
grep -rhoE '"[a-zA-Z]+\.[a-zA-Z0-9_]+"' --include="*.java" q-center/src q-executor/src \
  | sort | uniq -c | sort -rn | awk '$1>1'
```

**예방**: 종료 처리는 **한 메서드로 모은다**. 분기별로 복사하지 않는다.

---

### P4. DB 제약과 애플리케이션 의도 불일치

**정의**: 코드는 "일부만 지우고 나머지는 남긴다" 를 의도하는데, FK 제약이 그걸 허용하지 않아 **트랜잭션 전체가 실패**하는 경우.

**사례**
- 결함⑤ (DEF-01) — `approve.xml:251` 이 `APRV_YN='N'` 으로 "미승인 용어만 삭제, 승인 용어는 보존" 을 의도. 그러나 `tb_terms_words_fk_2` 에 `ON DELETE` 절이 없어(NO ACTION), 승인 용어가 참조 중이면 단어 삭제가 FK 위반 → **롤백**. 의도가 성립하지 않을 뿐 아니라 raw `PSQLException` 이 사용자에게 노출됨

**탐색 명령**
```bash
# ON DELETE 절이 없는 FK 목록 (NO ACTION = 참조 있으면 삭제 차단)
docker exec -i dataq-db psql -U admin -d postgres -c "\pset pager off" -c \
"SELECT conrelid::regclass AS child, conname, pg_get_constraintdef(oid) AS def
   FROM pg_constraint WHERE contype='f' AND connamespace='quality'::regnamespace
    AND pg_get_constraintdef(oid) NOT LIKE '%ON DELETE%' ORDER BY 1;"

# 그 다음: 그 부모 테이블을 DELETE 하는 매퍼를 찾아 가드 유무 확인
grep -rn "DELETE FROM TB_WORD\|DELETE FROM TB_TERMS\|DELETE FROM TB_DOMAIN" q-common/src/main/resources/mapper/
```

**예방**: 부모 행을 지우는 코드는 **FK 정의를 먼저 확인**하고, 차단되는 조건이면 **친화 메시지로 선제 차단**한다. `deleteWords` 의 참조 가드가 이 프로젝트의 표준 패턴.

---

### P5. 복합 키 일부 누락 — cascade/동기화가 엉뚱한 행까지 건드림

**정의**: PK 가 `(A, B, C)` 인데 WHERE 에 `(A, C)` 만 써서, 다른 `B` 값의 행까지 영향받는 경우.

**사례 (전부 `OBJ_OWNER` 누락)**
- `cascadeRejectAttrsInObj` — `DM_ID + OBJ_NM` 만 → 동명이 테이블이 다른 스키마에 있으면 그쪽 컬럼까지 반려
- `clearFkParentRefByAttr` — 동일
- `syncDataModelObjAttrCnt` — 동일 → 컬럼 수가 엉뚱하게 계산됨

**탐색 명령**
```bash
# OBJ_NM 을 쓰면서 OBJ_OWNER 를 안 쓰는 매퍼 구문 (데이터모델 영역 복합 PK)
grep -n "OBJ_NM" q-common/src/main/resources/mapper/stnd/*.xml \
  | grep -v "OBJ_OWNER" | grep -iE "where|and"
```

**판정 기준**: `TB_DATA_MODEL_OBJ` / `TB_DATA_MODEL_ATTR` 의 PK 는 `OBJ_OWNER` 를 포함한다. 조회/갱신 WHERE 에 없으면 의심.

**예방**: 복합 PK 테이블은 **PK 전체를 WHERE 에 쓴다**. `COALESCE(OBJ_OWNER,'')` 형태로 NULL 처리까지 통일.

---

### P6. 상태 전이 가드 부재 — 임의 상태로 점프 가능

**정의**: 상태 머신인데 UPDATE WHERE 에 **현재 상태 조건이 없어서** 어떤 상태에서든 어떤 상태로든 갈 수 있는 경우.

**사례**
- DEF-09 — `dmChangeHistory.xml` 의 `updateAprvStatus` WHERE 가 `CHANGE_SEQ` 뿐 → `APPROVED` → `REJECTED`, `REJECTED` → `APPROVED` 임의 전이 가능
- DEF-14 (관찰) — `approve.xml:236` 의 `DELETE FROM TB_WORD` 에 `APRV_YN` 조건 없음 → 승인된 항목도 반려 API 로 삭제 가능

**탐색 명령**
```bash
# 상태 컬럼을 SET 하는데 WHERE 에 그 컬럼이 없는 UPDATE
grep -n -A8 "SET.*APRV_STATUS\|SET.*APRV_YN\|SET.*STATUS" q-common/src/main/resources/mapper/stnd/*.xml \
  | grep -B6 "WHERE" | grep -L "APRV_STATUS =\|APRV_YN ="
```

**예방**: 상태 전이 UPDATE 는 `WHERE ... AND 현재상태 IN (허용집합)` 을 넣고, **영향 행 수 0 이면 거부**한다 (P2 와 결합).

---

### P7. 선언과 구현 불일치 — 주석·문서가 하겠다는 걸 코드가 안 함

**정의**: javadoc·설계서·컬럼 코멘트가 기술한 동작이 실제로는 없는 경우. 읽는 사람이 있다고 믿는다.

**사례**
- DEF-02 — `deleteObjs` javadoc "하위 컬럼·인덱스·제약조건 cascade 삭제" 인데 코드는 컬럼만 지움
- `TB_CHANGE_HISTORY.CHANGE_SOURCE` 컬럼 코멘트는 `ADMIN_DIRECT/USER_REQUEST/…` 를 기대하나 세팅 코드 0건 → 항상 NULL
- `datamodel.xml` 의 `<sql id="visibilityFilter">` — 정의만 있고 `<include refid>` 0건
- 88번 설계서의 `RENAME_OBJ`·`ADD_INDEX`·`MODIFY_ATTR_META` CHANGE_TYPE — 코드에 없음

**탐색 명령**
```bash
# (1) 정의만 되고 include 안 되는 sql 조각
for id in $(grep -ohE '<sql id="[^"]+"' q-common/src/main/resources/mapper/stnd/*.xml | sed 's/<sql id="//;s/"//'); do
  n=$(grep -c "refid=\"$id\"" q-common/src/main/resources/mapper/stnd/*.xml | awk -F: '{s+=$2} END{print s}')
  [ "$n" = "0" ] && echo "UNUSED sql fragment: $id"
done

# (2) 항상 NULL 인 컬럼 (코멘트는 값을 기대하는데)
docker exec -i dataq-db psql -U admin -d postgres -t -A -c \
"SELECT 'change_source', count(*) FILTER (WHERE change_source IS NOT NULL), count(*) FROM quality.tb_change_history;"

# (3) 문서에만 있는 엔드포인트/타입 — 94번 §3-4 역방향 대조 방식
```

**예방**: 주석이 cascade 를 약속하면 **그 cascade 를 검증하는 케이스를 같이 만든다** (96번 DM-040~043).

---

### P8. 필터 조건 누락 — 미승인·비활성 데이터 누출

**정의**: 승인/사용여부 필터가 어떤 조회 경로에는 있고 어떤 경로에는 없어서, 일관성이 깨지는 경우.

**사례**
- DEF-12 — `terms.selectAllTermsForDiag` 에 `APRV_YN` 필터 없음 → **미승인 용어가 진단 기준이 됨**. 반면 금칙어 체크는 `APRV_YN='Y'` 로 필터함 (비일관)
- DEF-10 — `syncAttrStndYnFromDiag` 에 `APRV_STATUS` 조건 없음 → 진단 모수엔 없던 DRAFT 컬럼이 `'Y'` 로 승격됨

**탐색 명령**
```bash
# 같은 테이블을 읽는데 승인 필터가 있는 쿼리 / 없는 쿼리 비교
grep -n "FROM TB_TERMS" q-common/src/main/resources/mapper/stnd/*.xml
grep -n "APRV_YN" q-common/src/main/resources/mapper/stnd/terms.xml
# 전자에는 있고 후자에 없는 statement 가 누출 후보

# 데이터모델 쪽
grep -c "COALESCE(APRV_STATUS" q-common/src/main/resources/mapper/stnd/datamodel.xml
grep -n "FROM TB_DATA_MODEL_ATTR" q-common/src/main/resources/mapper/stnd/datamodel.xml
```

**예방**: "이 테이블을 읽는 모든 경로" 를 먼저 열거하고 필터 정책을 **한 줄로 정의**한 뒤 전 경로에 동일 적용. 가능하면 `<sql>` 조각으로 공통화 (단 P7 처럼 정의만 하고 안 쓰면 무의미).

---

### P9. 부분 적용 — 정책이 일부 경로에만 걸림

**정의**: 승인·이력 같은 횡단 정책이 API 마다 제각각 적용된 경우. 우회로가 생긴다.

**사례 (88번 거버넌스)**
- `saveAttrs` ADD 만 완전 적용. **UPDATE 는 이력만 DRAFT, 모델 row 는 즉시 반영** (DEF-04)
- `saveAttrs` DELETE 는 **물리 삭제 즉시** → 반려해도 복구 불가 (DEF-05)
- `addAttr`/`deleteAttr`/`createPk`/`deletePk`/`createFk`/`deleteFk` — 거버넌스·이력 **둘 다 없음**
- `uploadAttrs`/`uploadTables`/import 계열 — 없음
- 수집은 `APRV_STATUS` 를 `APPROVED` 로 덮어씀

**탐색 명령**
```bash
# 정책 적용 지점을 세고, 해당 도메인의 변경 API 총수와 비교
grep -rn "changeHistory.record(" --include="*.java" q-center/src | wc -l     # 8곳
grep -rn "setAprvStatus(" --include="*.java" q-center/src                    # 2곳뿐
grep -cE '@(Post|Request)Mapping' q-center/src/main/java/qualitycenter/controller/DataModelController.java
# 변경 API 수 >> 정책 적용 수 이면 구멍
```

**예방**: 횡단 정책은 **인터셉터/AOP 또는 단일 서비스 메서드** 로 강제한다. 컨트롤러마다 손으로 부르면 반드시 빠진다.

---

### P10. 테스트 자산 부패 — 하드코딩 fixture 가 썩음

**정의**: 테스트가 특정 데이터의 존재·순서를 가정해서, 그 데이터가 바뀌면 **제품이 아니라 테스트가** 깨지는 경우.

**사례 (2026-08-22 전부 실측)**
- `"CAMS"` 모델 하드코딩 → `USE_YN='N'` 되며 autocomplete 에서 사라짐 (2개 파일)
- `단어등록TEST` 단어 → DB 에서 사라짐
- `TB_MEMEBER` 오타 테이블명 → `saveAttrs` 가 부모 OBJ 검사를 안 해 오타로도 컬럼이 생기는 바람에 Step 1~5 는 통과하고 Step 6 만 실패
- `arr[0]` 이 특정 모델일 거라는 가정 → 2개 파일

**탐색 명령**
```bash
# 테스트의 하드코딩 식별자
grep -rnE '"[A-Z_]{3,}"|"[가-힣]+TEST"' dataQ설계/테스트/selenium/*.py | grep -vE "^\S+:[0-9]+:\s*#"
grep -rn "arr\[0\]\|\[0\]\[" dataQ설계/테스트/selenium/*.py
```

**예방**: 필요한 **속성으로 조회**한다 (`clctDt` 있는 모델, `word_clsf_yn='Y'` 인 단어). 없으면 **테스트가 직접 만든다**.

---

### P11. 트랜잭션 경계 부재 — 다건 변경이 부분 반영됨

**정의**: 논리적으로 하나인 작업을 여러 문장으로 나눠 실행하는데, **문장 단위 자동 커밋**이라 중간 실패 시 앞부분만 커밋되는 경우. 롤백이 없다.

**사례 (2026-08-23 확인)**
- `DataModelController.updateObj` — 테이블 rename cascade **5개**(`:1474-1478`)와 OWNER 변경 cascade **4개**(`:1488-1491`)가 전부 `sqlSessionTemplate`
  - 3번째에서 실패하면 1·2번은 커밋된 채로 남음
  - 결과: 컬럼은 새 이름, 인덱스는 옛 이름 → **영구 불일치**. 재실행해도 WHERE 가 옛 이름을 못 찾아 복구 불가

**분포**: `sqlSessionFactory.openSession()` 19곳 vs `sqlSessionTemplate.(update|insert|delete)` 121곳. 어디가 트랜잭션인지 전수 파악 안 됨.

**왜 통합테스트로 못 잡나**: 중간 실패를 인위적으로 만들어야 한다(제약 위반·연결 끊김). **정적 조사가 맞는 방법.**

**탐색 명령**
```bash
# 한 메서드 안에서 sqlSessionTemplate 변경 문장이 2회 이상 → 트랜잭션 후보
awk '/^\t(public|private|protected) /{m=$0; c=0}
     /sqlSessionTemplate\.(update|insert|delete)\(/{c++; if(c==2) print FILENAME": "NR": "m}' \
  q-center/src/main/java/qualitycenter/controller/*.java

# cascade/rename 계열이 트랜잭션 밖인지
grep -n 'sqlSessionTemplate.update("datamodel.\(cascade\|rename\)' \
  q-center/src/main/java/qualitycenter/controller/DataModelController.java
```

**판정 기준**: 한 요청에서 **2개 이상의 테이블을 바꾸는데** `openSession` 이 아니면 결함.

**예방**: 다건 변경은 `sqlSessionFactory.openSession()` + `commit()`/`rollback()`. `deleteObjs`(`:1583-1645`)가 이 프로젝트의 올바른 예 — 단일 세션, 1건 실패 시 전체 롤백.

---

### P12. 동시성 가드 부재 — 같은 대상에 동시 작업

**정의**: 같은 리소스를 동시에 바꾸는 요청을 막는 장치가 없는 경우. 단일 사용자 테스트로는 절대 안 잡힌다.

**사례 (2026-08-23 확인)**

| 작업 | 중복 방지 |
|---|---|
| 품질 진단 | 컬럼 단위 mutex(`TB_QUAL_RUNNING_LOCK`) + 글로벌 Semaphore(5) |
| 표준 진단 | Job 단위 409 (`selectActiveDiagJobCount`) |
| 구조 진단 | 409 |
| **데이터모델 수집** | **가드 0건** |

수집은 MERGE 라 동시 실행 시 서로의 결과를 덮어쓴다. `TB_DATA_MODEL_CLCT` 도 2행 생기고 진단이 어느 쪽을 잡을지 불명확.

**탐색 명령**
```bash
# 장시간 실행 API 중 중복 가드 없는 것
for ep in collectDataModel startDiag structDiag/execute; do
  echo "--- $ep"
  grep -rn -A15 "\"/$ep\"\|\"$ep\"" q-center/src/main/java/qualitycenter/controller/*.java \
    | grep -icE "running|중복|already|409|Count" 
done
```

**예방**: 장시간 실행 + 상태 변경 API 는 **진행 중 여부 조회 → 409** 를 기본으로. 이미 `DiagController:93-97` 에 좋은 예가 있다.

---

### P13. 환경 구축 스크립트 드리프트

**정의**: 신규 환경을 만드는 스크립트가 현재 스키마와 어긋나서, 새 PC·CI 에서만 터지는 경우. 기존 환경에서는 절대 안 보인다.

**사례 (2026-08-23 확인)**
- `dataq-db/initdb/01_schema.sql` (2026-04-21) vs `dataQ설계/DDL_full_schema.sql` (2026-05-15)
- initdb 에 `change_source` 컬럼 **0건**, `tb_term_resolve_history` 테이블 **0건**
- → 이 파일로 초기화하면 `insertChangeHistory`·`termResolve.insert` 가 **전부 실패**

**탐색 명령**
```bash
# 초기화 스크립트 ↔ 현재 스키마 테이블 대조
grep -ohiE 'CREATE TABLE [a-z.]*\btb_[a-z0-9_]+' dataq-db/initdb/01_schema.sql \
  | sed 's/.*\.//' | tr 'A-Z' 'a-z' | sort -u > /tmp/init_tbl.txt
grep -ohE '^CREATE TABLE quality\.[a-z_0-9]+' dataQ설계/DDL_full_schema.sql \
  | sed 's/.*\.//' | sort -u > /tmp/live_tbl.txt
comm -13 /tmp/init_tbl.txt /tmp/live_tbl.txt   # initdb 에 없는 테이블
```

**예방**: 초기화 소스를 **하나로 통일**한다. CLAUDE.md 의 "DDL 단일 진실" 정책이 `DDL_full_schema.sql` 을 지목하고 있으므로 initdb 를 별도 유지할 이유가 없다.

---

## 3. 결함 대장

| # | 결함 | 패턴 | 상태 | 발견 경로 |
|---|---|---|---|---|
| ① | `terms.selectTermsInfo` 미정의 → 용어 승인 변경이력 `curr_value` 항상 NULL (실측 18건 중 16건) | P1 | **수정·검증 완료** | 정적 대조 |
| ② | `updateAttr`/`updateObj`/`deleteAttr` 0건 갱신에도 200 | P2 | **수정·검증 완료** | UI 테스트 추적 |
| ③ | 스케줄 ERROR 마감 경로 2곳이 `LAST_EXEC_*` 미갱신 | P3 | **수정·검증 완료** | UI 테스트 추적 + DB 실측 |
| ④ | `impact.selectImpactColumnsByWordId` 가 삭제된 `DM_CLCT_ID` 참조 → 단어 영향도 분석 항상 500 | P1 | **수정·검증 완료** | API 일괄 실호출 |
| ⑤ | 승인 용어 참조 시 단어 반려가 FK 위반으로 전체 롤백 + raw 예외 노출 | P4 | **수정 완료, 빌드 대기** | 통합 시나리오 실측 |
| ⑩ | **진단 결과가 `OBJ_OWNER` 를 보관하지 않아 동명이 테이블의 이슈가 교차 집계됨** | P5 | **실증 확인, 미조치** | 매뉴얼 스크린샷 육안 → SQL 검증 |
| DEF-02 | `deleteObj`/`deleteObjs` 가 INDEX/CONSTRAINT/inbound FK/`FK_PARENT_OBJ_NM` 미정리 (javadoc 과 불일치) | P7 | **수정·검증 완료** (2026-08-23) | 소스 추적 → 실측 |
| DEF-03 | 테이블 rename 시 `FK_PARENT_OBJ_NM` 미갱신 | P5 | **수정·검증 완료** (2026-08-23) | 소스 추적 → 실측 |
| DEF-04 | `saveAttrs` UPDATE 는 이력만 DRAFT, 모델 row 즉시 반영 | P9 | **수정·검증 완료** (2026-08-23) — 반려 시 `BEFORE_JSON` 으로 원복 | 소스 추적 → 실측 |
| DEF-05 | `DEL_ATTR`/`DEL_OBJ` 승인 전 물리 삭제 → 반려해도 복구 불가 | P9 | **수정·검증 완료** (2026-08-23) — 대기 삭제는 `USE_YN='N'`, 승인 시 확정 / 반려 시 복구 | 소스 추적 → 실측 |
| DEF-06 | 반려 cascade 1단계만 (손자 미전파) | P9 | **수정·검증 완료** (2026-08-23) — 큐 기반 반복 전파 | 소스 추적 → 실측 |
| DEF-07 | cascade 반려가 모델 row 만 갱신, 이력은 `SUBMITTED` 잔존 | P9 | **수정·검증 완료** (2026-08-23) | 소스 추적 → 실측 |
| DEF-08a | `syncDataModelObjAttrCnt` 의 `OBJ_OWNER` 누락 | P5 | **결함 아님** — WHERE 는 느슨하나 SET 서브쿼리가 `A.OBJ_OWNER = O.OBJ_OWNER` 로 상관 결합. 결과는 정확, 불필요한 write 만 | 실측으로 반증 |
| DEF-08b | `clearFkParentRefByAttr` 의 `OBJ_OWNER` 누락 | P5 | **스키마 한계** — `FK_PARENT_OBJ_NM` 컬럼 자체에 owner 정보가 없어 구분 불가. 컬럼 추가 없이는 해결 불가 | 소스 추적 |
| DEF-08c | `cascadeRejectAttrsInObj` 의 `OBJ_OWNER` 누락 | P5 | **수정·검증 완료** (2026-08-23) | 소스 추적 → 실측 |
| DEF-09 | `updateAprvStatus` 현재상태 가드 없음 | P6 | **수정·검증 완료** (2026-08-23) — `fromStatuses IN ('DRAFT','SUBMITTED')`, 0건이면 예외 | 소스 추적 → 실측 |
| DEF-10 | `syncAttrStndYnFromDiag` 가 DRAFT 컬럼을 `'Y'` 승격 | P8 | **수정 완료** (2026-08-23) — `APRV_STATUS='APPROVED'` 조건 추가 | 소스 추적 → 실측 |
| DEF-11 | `resolveAttrs` 가 `ATTR_NM` 변경 시 rename cascade 미호출 | P9 | **실증 미완** — 코드 근거는 유효하나(`:2568` vs `saveAttrs :2248-2251`), 변환이 검증 단계(`데이터 길이가 표준 도메인과 다릅니다 (표준: 100, 입력: 0)`)에서 막혀 rename 까지 도달 못 함. 재시도 필요 | 소스 추적 |
| DEF-12 | 미승인 용어가 진단 기준에 포함 | P8 | **수정·검증 완료** (2026-08-23) — 진단·표준화 5경로를 `selectApproved*` 로 교체. 중복검사용 쿼리는 그대로 | 소스 추적 → 실측 |
| DEF-13 | `reqTp='CODE'` 는 구성단어 승인 가드를 안 탐 | P9 | **수정·검증 완료** (2026-08-23) | 소스 추적 |
| DEF-14 | 승인된 항목도 반려 API 로 삭제 가능 (가드 부재) | P6 | **수정·검증 완료** (2026-08-23) — DEF-09 와 동일 가드 | 소스 추적 |
| DEF-15 | `putStdAprvStat` 에 관리자 체크 없음 (Security 룰 미확인) | P9 | **수정·검증 완료** (2026-08-23) — 403 반환 실측 | 소스 추적 |
| — | `CHANGE_SOURCE` 항상 NULL (404행 전부) | P7 | **수정 완료** (2026-08-23) — 세션 권한에서 파생 + 승인/추천/일괄은 명시. 화면 '등록 경로' 컬럼 복원 | 소스 추적 → DB 실측 |
| — | 수집 동시성 가드 부재 | P12 | **수정 완료** (2026-08-23) — `selectActiveCollectCount` (6시간 stale 무시) | 제안 |
| — | PostgreSQL 수집 쿼리 부재 (9종 표방 vs 실제 3종) | P7 | **수정 완료** (2026-08-23) — Objs/Attrs/Indexes/Constraints 4종 + 스키마 목록 | 제안 |
| — | `tb_qual_col_rule`/`_rule_result`/`_running_lock` 에 `OBJ_OWNER` 없음 | P5 | **수정 완료** (2026-08-23) — 컬럼 추가 + 매퍼 조인 5곳 | 결함 ⑩ 파생 |
| — | 반려가 `TB_CHANGE_HISTORY` 미기록 | P7 | 사양 확인 필요 — 반려는 "없던 일" 이므로 미기록이 맞다는 해석도 가능 | 소스 추적 |

---

## 4. 조치 상세 (수정 완료분)

### ① `terms.selectTermsInfo` 미정의

| | |
|---|---|
| **증상** | 용어 승인 시 `TB_CHANGE_HISTORY.CURR_VALUE` 가 항상 NULL |
| **실측 피해** | `CHANGE_TYPE='INSERT'` 기준 — DOMAIN 23/23 정상, WORD 159/161 정상, **TERM 2/18 (89% 유실)**. 3개월 누적 |
| **원인** | `DataStandardController` 가 `terms.selectTermsInfo` 를 호출하나 `terms.xml` 에는 `selectTermsInfoByNm` 만 존재. 바깥 `catch (Exception ex) { log.warn(...) }` 가 삼켜서 승인은 성공 |
| **조치** | `terms.xml` — 기존 본문을 `<sql id="termsInfoSelectBody">` 로 추출, 같은 본문의 `selectTermsInfoById` 신설. 컨트롤러를 `word.selectWordInfoById` 분기와 동일 형태(`selectList` + `get(0)` + `targetNm` 보정)로 정렬 |
| **검증** | 용어 등록 → 승인 실행 → `curr_value` = `StdTermsVo(id=…, termsNm=API가격, …)` 기록 확인. `getTermsInfoByNm` 회귀 200/1건 |
| **교훈** | **예외를 삼키는 catch 는 결함을 3개월 숨긴다.** 삼킬 거면 최소한 무엇이 비었는지 알 수 있는 단언을 어딘가에 둬야 한다 |

### ② 0건 갱신에도 200

| | |
|---|---|
| **증상** | `objOwner` 없이 `/api/dm/updateAttr` 호출 시 값이 안 바뀌는데 `resultCode=200` |
| **원인** | MyBatis `update()` 반환값(영향 행 수) 미검사. 매퍼 WHERE 는 `OBJ_OWNER` 를 키에 포함 |
| **조치** | `updateAttr`/`updateObj`/`deleteAttr` 세 메서드에 영향 행 수 검사 추가. 0이면 `CODE_500` + 대상 식별자(dmId/owner/obj/attr) 메시지. `deleteAttr` 은 `session.rollback()` 후 반환 |
| **검증** | `objOwner` 누락 → 500 + "수정 대상 컬럼을 찾지 못했습니다. (dmId=…, owner=null, obj=TB_CPN, attr=CPN_NO)" / DB 불변. `objOwner` 포함 → 200 + DB 반영. 검증 후 원복 |
| **교훈** | 같은 프로젝트의 `DiagTargetController` 는 이미 `success`/`count` 를 정직하게 돌려주고 있었다. **좋은 패턴이 옆에 있는데 안 따른 것** |

### ③ 스케줄 ERROR 마감 시 `LAST_EXEC_*` 미갱신

| | |
|---|---|
| **증상** | 실행 실패한 스케줄의 "최종 실행" 이 목록에서 영원히 빈칸 |
| **원인** | 로그 마감 경로 3곳 중 `DiagSchedulerRunner.markFinish` 만 `updateScheduleLastExec` 를 호출. ERROR 마감 2곳(`DiagScheduleController`, `ScheduledDiagLauncher`)은 누락 |
| **조치** | ERROR 마감 2곳에 `updateScheduleLastExec(status='ERROR', logId)` 추가 |
| **검증** | 수집 이력 없는 모델로 `runNow` → `last_exec_status='ERROR'`, `last_exec_dt`, `last_exec_log_id` 기록. **UI 스케줄 목록에 `ERROR` 배지 노출까지 스크린샷으로 확인** |
| **교훈** | 종료 처리가 3곳으로 흩어져 있으면 하나는 반드시 빠진다 |

### ④ 단어 영향도 분석 500

| | |
|---|---|
| **증상** | `GET /api/std/impact/word` 항상 500 — `column a.dm_clct_id does not exist` |
| **원인** | `impact.xml` 이 `a.DM_CLCT_ID` 로 최신 수집 회차를 골랐는데, CLCT 폐기 때 `TB_DATA_MODEL_ATTR` 에서 그 컬럼이 제거됨 (`information_schema` 0건). 형제 쿼리들은 안 써서 `impact/domain` 만 살아 있었음 |
| **조치** | 현재 규약(`DM_ID` + `USE_YN='Y'`)으로 대체, 모델도 `m.USE_YN='Y'` 로 제한 |
| **검증** | `고객` → 용어 28 / 컬럼 21건, `코드` → 128 / 4097건, `번호` → 543 / 1252건. `impact/domain` 회귀 정상. 셀레니움 46개 전건 재통과 |
| **교훈** | **셀레니움 46개가 전부 초록인 상태에서 죽어 있었다.** 화면이 안 부르는 경로는 UI 테스트가 못 본다 → API 일괄 실호출을 정식 계층(L2)으로 만든 이유 |

### ⑤ 승인 용어 참조 시 단어 반려 실패

| | |
|---|---|
| **증상** | 승인된 용어가 참조 중인 단어를 반려하면 `resultCode=500` + raw `PSQLException` 노출, 트랜잭션 전체 롤백 |
| **실측** | 단어 잔존 1 / 미승인용어 잔존 1 / 승인용어 잔존 1 / 반려이력 0 — 아무것도 안 됨 |
| **원인** | `tb_terms_words_fk_2` 에 `ON DELETE` 절 없음(NO ACTION). `approve.xml:251` 의 `APRV_YN='N'` 은 "승인 용어 보존" 의도이나, FK 때문에 **반려 자체가 불가능** |
| **조치** | `approve.xml` 에 `selectApprovedTermsByWordId` 신설. 반려 분기에서 물리삭제 **이전에** 승인 용어를 조회해, 있으면 `deleteWords` 와 동일 형태의 친화 메시지로 차단 |
| **검증** | 빌드 후 IT-APV-016~018 로 확인 예정 |
| **교훈** | **애플리케이션 의도와 DB 제약이 어긋나면 의도가 진다.** 부모 행을 지우는 코드는 FK 정의를 먼저 봐야 한다 |

---

### ⑩ 진단 결과가 OBJ_OWNER 를 보관하지 않아 동명이 테이블 이슈가 교차 집계 (2026-08-23 조치 완료)

| | |
|---|---|
| **증상** | 진단 결과 화면 "전체 현황" 에 **전체 테이블 19개 / 이슈 테이블 22개** — 이슈가 전체보다 많다. "이슈 TOP 5" 에 `TB_USER` 가 1·2·3위로 **같은 수치(11컬럼/14이슈)로 3번** 노출 |
| **발견 경로** | 매뉴얼용 스크린샷을 **육안으로 보다가** 발견. 셀레니움 47개·API 84건 어디에도 안 걸렸다 |
| **원인** | `TB_DIAG_RESULT` 에 `obj_owner` 컬럼이 **없다** (`result_id, diag_job_id, obj_nm, attr_nm, attr_nm_kr, diag_type, diag_detail, std_value, actual_value`). `diag.xml:198-200` 의 `selectDiagResultSummary` 가 `AND R.OBJ_NM = O.OBJ_NM` 만으로 LEFT JOIN 하므로, 같은 이름의 다른 스키마 테이블이 **서로의 이슈를 그대로 물려받는다** |
| **실측 (오라클테스트 모델, 스키마 3개)** | 동명이 테이블: `TB_USER` 3개(HRM_APP/INV_APP/SALES_APP), `COMPANY_INFO` 2개(INV_APP/SALES_APP)<br>실제 진단 결과: `TB_USER` 11컬럼/14건 **1건**, `COMPANY_INFO` 8컬럼/8건 **1건**<br>화면 표시: `TB_USER` 11/14 가 **3행**, `COMPANY_INFO` 8/8 이 **2행**<br>OBJ 행 수 22 vs 고유 이름 19 → "전체 19 / 이슈 22" 의 정체 |
| **파급** | 준수율 산식도 `COUNT(DISTINCT OBJ_NM \|\| '.' \|\| ATTR_NM)` 로 owner 를 안 보므로, **다른 스키마의 같은 이름 컬럼이 1건으로 합쳐져 이슈 컬럼 수가 과소 집계**된다. 다중 스키마 수집이 기본 사용 형태라 영향이 작지 않다 |
| **조치** | **DB** — `TB_DIAG_RESULT.OBJ_OWNER VARCHAR(100)` 추가 + 인덱스. 기존 7,246행 중 (모델,테이블명) 이 소유자 하나뿐인 6,187행 백필, 다중 스키마 중복 1,059행은 복원 정보가 없어 NULL 유지 (해당 과거 JOB 은 재진단해야 정확)<br>**Executor** — `DiagService.buildResult` 가 `attr.getObjOwner()` 저장<br>**매퍼** — `diag.xml` 요약·상세 JOIN 에 owner 조건, `objNmKr` 서브쿼리에도 owner, `ISSUE_COL_CNT` DISTINCT 키에 owner. `datamodel.xml` 의 모델 현황 준수율·`syncAttrStndYnFromDiag` 동일 처리. `diagTarget.xml` 통계 서브쿼리 3곳<br>**프론트** — `DSDataDiagResult.overviewStats` 의 `totalTableCnt` 가 이름만으로 중복 제거해 `issueTableCnt` 보다 작아지던 것 수정 (이게 "19 / 22" 의 직접 원인) |
| **검증** | `dataQ설계/테스트/selenium/verify_diag_owner.py` — 진단 재실행 후 **요약 집계 = 실제 저장 건수** 인지 본다. 수정 전 146 vs 저장 110 (팬아웃), 수정 후 110 = 110. 화면도 "전체 22 / 이슈 22" 로 정합. `COMPANY_INFO` 가 `SALES_APP=5 / INV_APP=3` 으로 분리 |
| **함께 발견해 고친 같은 계열** | `DSDiagTargetMgmt` 테이블 선택기가 `item-value="objNm"` 이라 소유자를 잃어 다른 스키마 컬럼까지 표시 → `return-object` + `OWNER.TABLE` 라벨<br>`DSDatamodelStatusColumn` 의 표준화 응답 매칭·실패사유 맵·한글명 중복검사 키 6곳 (다른 스키마의 같은 한글명이 **중복 오탐**으로 저장을 막고 있었다) → 서버 응답에 `objOwner` 추가 후 키에 반영<br>`DSDatamodelVisualization` FK 부모 조회가 같은 이름 아무거나 집던 것 → 같은 스키마 우선 |
| **남은 것** | `tb_qual_col_rule` / `tb_qual_rule_result` / `tb_qual_running_lock` 에는 `OBJ_OWNER` 자체가 없다. 같은 설계 결함이지만 품질 진단 메뉴가 비활성이라 이번 범위에서 제외 |
| **교훈** | **스크린샷을 육안으로 보는 것도 검증이다.** 자동 테스트 47개가 전건 통과하는 상태에서, 사람이 화면을 한 번 보자마자 "이슈가 전체보다 많다" 가 눈에 띄었다. 숫자의 의미적 정합성(A ≤ B 같은 불변식)은 기계가 안 보면 못 잡는다<br>**그리고 식별 키를 바꿀 땐 그 키로 조인되는 모든 곳을 훑어야 한다.** 2026-05-09 에 OBJ/ATTR 의 PK 에 OWNER 를 넣으면서 그걸 참조하는 진단 결과·품질 룰 테이블을 빠뜨렸고, 3개월간 준수율이 틀린 채 표시됐다. 점검 명령은 `DDL_변경이력.md` 사고 케이스에 기록 |

---

### 회귀 6건 오진 사례 (2026-08-23)

거버넌스 수정 후 회귀에서 6건이 깨졌다. 원인은 셋으로 갈렸고, **내 코드 결함은 1건뿐**이었다.

| 실패 | 진짜 원인 | 교훈 |
|---|---|---|
| `test_qual_col_rule` 0/15, `test_qual_rule_result` 5, `test_qual_diag_progress` 4 | **내 실수** — 조인에 `cm.OBJ_OWNER` 를 썼는데 `col_meta` CTE 가 그 컬럼을 안 뽑았다 | CTE 를 거쳐 참조되는 컬럼은 **CTE 정의부터** 확인한다. 테이블에 컬럼이 있다고 CTE 에도 있는 게 아니다 |
| `test_qual_lock` P1 | **테스트 하드코딩** — 컬럼 개수를 `== 6` 으로 고정. `OBJ_OWNER` 추가로 7이 됨 | 스키마 검증은 개수가 아니라 **필수 컬럼 존재**로 한다. 개수 고정은 무관한 변경에 깨진다 |
| `test_88_collect_tablespace` 전체, `test_phase4_schedule_coverage` E | **내 기동 실수** — 서버를 `target/` 에서 띄워 `lib/drivers.xml` 을 못 찾음 | 아래 참조 |

세 번째가 특히 위험했다. 증상이 "수집이 시작조차 안 됨 (수집 이력 row 자체가 없음)" 이라
바로 직전에 넣은 **수집 동시성 가드를 범인으로 의심**했다. 실제로는 JDBC 드라이버 로딩 실패였고,
가드는 무관했다. DB 조회 기능은 전부 정상이라 서버가 멀쩡해 보인 것도 오진을 도왔다.

> **증상이 방금 넣은 코드와 닮았다고 그게 원인은 아니다.**
> 가드를 의심했으면 가드가 실제로 몇 건을 반환했는지부터 봤어야 했다.
> 확인해 보니 해당 모델의 수집 이력 자체가 0건이라 가드는 애초에 0을 반환할 수밖에 없었다.
> 이 한 줄짜리 확인이 오진을 갈랐다.

기동 시 반드시 모듈 루트에서 — `cd q-center && java -jar target/q-center-*.jar`.
`target/` 에서 띄우면 연결 테스트가 `DriverVo.getDriverName() ... bean is null` 로 실패하고
스키마 목록이 0개가 된다.

### 2차 회귀에서 하나 더 (같은 날)

수정 후 재실행에서 `test_qual_col_rule` P8 하나가 남았다. 이번엔 진짜 내 결함이었다.

**조회 조인에만 소유자를 넣고 쓰기 경로를 안 고쳤다.** `TB_QUAL_COL_RULE` 의 PK 는
`(DM_ID, OBJ_NM, ATTR_NM)` 이고 upsert 도 소유자를 안 넣어 `OBJ_OWNER` 가 NULL 로 들어갔다.
반면 조회는 `COALESCE(cr.OBJ_OWNER,'') = COALESCE(cm.OBJ_OWNER,'')` 를 요구하니
`'' ≠ 'HRM_APP'` 로 매칭이 깨졌다. **저장은 200 이 뜨는데 화면에 반영이 안 되는** 최악의 형태다.

단독 실행 때 통과했던 건 백필된 행(46건 중 45건)이 이미 소유자를 갖고 있었기 때문이다.
스위트에서는 새로 INSERT 되며 NULL 이 들어가 드러났다.

> **식별 키에 컬럼을 추가하면 읽기·쓰기가 한 세트다.**
> INSERT · ON CONFLICT 대상 · DELETE WHERE · PK 제약 · VO · 컨트롤러까지 같이 간다.
> 하나라도 빠지면 "쓰기는 되는데 안 보인다" 가 된다. 조인만 고치고 끝내지 말 것.

호출부가 소유자를 안 보내는 경우를 위해 서버에서 모델을 보고 해석하되,
**여러 스키마에 같은 이름이 있으면 임의로 고르지 않고 400 으로 거부**하도록 했다.
모호한 걸 조용히 하나 고르는 게 애초에 이 결함군의 뿌리였다.

### 3차 회귀 — 폴루션 정리가 처음부터 죽어 있었다

3차에서 `test_reject_physical_delete` 가 4건 실패했다. 단독 실행은 6/6 통과.
"플레이키" 로 넘길 뻔했는데, 원인이 테스트 인프라에 있었다.

`run_all.py` 의 `CLEANUP_SQL` 첫 문장이 `TB_TERMS_WORDS.TERMS_NM` 을 참조하는데
그 테이블엔 `TERMS_ID` 밖에 없다. `psql -c` 는 여러 문장을 **하나의 암시적 트랜잭션**으로
묶으므로 첫 에러에 전부 롤백된다. 게다가 `cleanup_db()` 는 결과를 안 보고 예외도 삼켰다.

> **폴루션 정리가 한 번도 실행된 적이 없었다.**

증거: `TB_WORD` 에 `셀반려삭제NNN` 이 **17건**, 어제 것까지 쌓여 있었다.
테스트는 3자리 난수(900가지) suffix 를 쓰므로 잔여물이 쌓이면 충돌이 나고,
"이미 존재하는 단어" 로 STEP 1 부터 깨진다. 실행 횟수가 늘수록 실패 확률이 올라간다.

조치
1. `CLEANUP_STATEMENTS` 로 분리해 **문장별 독립 실행** — 하나 실패해도 나머지는 지운다
2. 컬럼명을 실제 스키마에 맞춤 (`TERMS_ID` 서브쿼리 + `WORD_NM` 직접 조건)
3. 스위트 시작 시 `verbose=True` 로 **실패한 문장을 출력** — 다시 조용히 죽지 않게
4. 난수 suffix 를 `시각(HHMMSS)+난수` 로 교체 (5개 파일) — 충돌 자체를 제거

> **정리(cleanup)·설정(setup) 코드가 실패해도 아무도 모르는 구조를 만들지 말 것.**
> 삼킨 예외 하나가 며칠치 폴루션을 만들고, 그게 "간헐 실패" 로 위장한다.
> 간헐 실패를 만나면 테스트를 의심하기 전에 **정리 코드가 실제로 도는지부터** 확인한다.

---

## 5. 새 결함을 만났을 때

1. **§2 패턴 목록에서 유사 패턴 검색** — 대부분 이미 있다
2. 패턴이 있으면 **그 패턴의 탐색 명령을 돌려 같은 유형이 더 있는지 확인** (1건 고치고 끝내지 말 것)
3. 패턴이 없으면 §2 에 추가 — **탐색 명령을 반드시 같이 적는다**
4. §3 대장에 1행 추가
5. 수정했으면 §4 에 상세 + **교훈** 1줄
6. 96번에 회귀 케이스 추가 (`★` 표시)

### 발견 경로별 권장 순서

| 순서 | 방법 | 잡히는 유형 |
|---|---|---|
| 1 | 정적 대조 (호출↔정의, 컬럼↔스키마) | P1 |
| 2 | API 일괄 실호출 (읽기 전용) | P1, P8 |
| 3 | 소스 상태전이·cascade 추적 | P5, P6, P7, P9 |
| 4 | 통합 시나리오 실측 | P2, P3, P4 |
| 5 | UI 테스트 | 화면 회귀 |

**UI 테스트가 마지막인 이유**: 비용이 가장 크고 잡히는 범위가 가장 좁다. 46개 전건 통과 상태에서 결함 ④·⑤ 가 살아 있었다.
