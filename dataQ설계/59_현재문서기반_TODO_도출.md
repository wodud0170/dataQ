# 59. 현재 설계 문서 기반 TODO 도출

**작성일**: 2026-04-22
**작성 범위**: 진행 중/미완으로 남아있는 항목을 설계 문서에서 추출. 완료 항목은 제외.
**근거 문서**: 50, 56, 57, 47, 45, 36, 44

---

## 0. 현재 상태 한 줄 요약

- CLCT 폐기 — ✅ 전 구간 완료 (OBJ/ATTR/INDEX/CONSTRAINT)
- 논리모델 컬럼 추가 재설계 — Phase A/B ✅, Phase C/D ⏳
- 빌드 — 2026-04-22 09:58 성공 (q-common/q-center/q-executor 세 모듈 모두 repackage 완료)
- DDL 싱크 — PC1 DB: ✅ (2026-04-21 INDEX/CONSTRAINT 재구성까지 반영)

---

## 1. 최우선 — 서버 재기동 후 동작 검증 [출처: 50번 §4-8 체크리스트 5~7]

빌드만 끝났고 기동·검증은 안 된 상태.

| # | 항목 | 방법 | 기대 결과 |
|---|------|------|----------|
| 1-1 | **컬럼 추가 (정상 매칭)** | 수동: `고객명`/`등록일시`/`전화번호` 입력 → [표준 변환] | 초록 패널 + readonly에 물리명·타입 자동 채움 + 저장 성공, 그리드 정상 표시 |
| 1-2 | **컬럼 추가 (비표준)** | 수동: `사용자명` 또는 `주소` 입력 → [표준 변환] | 빨간 패널 + [용어 등록하기]/[비표준으로 추가] 2버튼 노출 |
| 1-3 | **비표준 저장** | 1-2에서 [비표준으로 추가] 클릭 | 그리드 행 빨간 배경 + ⚠ 아이콘 + tooltip "비표준 용어" |
| 1-4 | **resolveStandard 응답 필드** | 개발자 도구 네트워크 탭 | `termsId`, `domainId`, `domainMissing` 포함 확인 |
| 1-5 | **논리 모델 CLCT 참조 null 예외 부재** | addAttr/updateAttr/deleteAttr, updateObj/deleteObj 수동 실행 | "최신 스냅샷을 찾을 수 없습니다" 미발생 |

---

## 2. Phase C — 용어 등록 유도 연동 [출처: 50번 §4-7, §5 미구현]

Phase B에서 [용어 등록하기] 버튼은 `eventBus.$emit('openTermsRegister')` 까지만 붙어있고 수신측이 없음.

| # | 작업 | 파일 |
|---|------|------|
| 2-1 | NdNav.vue `mounted()` 에 `eventBus.$on('openTermsRegister', ...)` 수신자 추가 | `q-center/vue/front/src/views/nav/NdNav.vue` |
| 2-2 | 용어 등록 다이얼로그를 모달-in-모달로 열거나 별도 탭 이동 (addTabItem) | 동 상 |
| 2-3 | 용어 등록 완료 시 `eventBus.$emit('termsRegistered', {nm})` → DSDatamodelStatusColumn.vue 에서 수신 후 자동 재변환 | `DSDatamodelStatusColumn.vue` |
| 2-4 | 한글명 pre-fill 확인 (eventBus.pendingTermsRegister 패턴) | 동 상 |

**산출물**: 용어 없음 → 등록 → 컬럼 다이얼로그 복귀 → 재변환까지 끊김 없음

---

## 3. Phase D — 일괄 재변환 + 논리/물리 구분 재검토 [출처: 50번 §4-9, §5 미구현]

### 3-1. 일괄 재변환
- 이미 백엔드 `/api/dm/resolveAttrs` 는 PC2 커밋에서 구현됨 (56번 §6-4)
- 프론트: 그리드 상단에 `[선택 컬럼 물리모델 변환]` 버튼은 존재. **비표준 행 전체 선택 + 일괄 변환 실행** UX 점검만 필요
- 검증 필요: 한글 용어가 나중에 승인됐을 때 재변환 시 `resolveReason` 이 비워지고 `TERMS_STND_YN='Y'` 로 바뀌는지

### 3-2. 44번 설계 업데이트
- 44번은 여전히 "논리 모델은 별도 편집 모드" 전제로 작성됨
- 실제 구현은 변환 단일 경로로 통일됐으므로 44번 문서를 현재 상태로 갱신
- 대안: 44번 상단에 "Deprecated — 50번 참고" 배너만 추가해도 됨

---

## 4. 미정리 기능 [출처: 56번 §12]

| # | 항목 | 현재 상태 | 남은 일 |
|---|------|----------|--------|
| 4-1 | 인덱스/제약조건 **편집 UI** | 조회 전용 (`DSDatamodelStatusIndex/Constraint.vue`) | 논리 모델에서 PK/FK/UK/CHECK/Index 직접 설계하는 UI 설계·구현. DDL 생성 시 포함되려면 필요 |
| 4-2 | **변환 불가 사유 영속화** | `resolveReason` 을 응답 세션에만 보관 → 재조회 시 사라짐 | TB_DATA_MODEL_ATTR 에 `RESOLVE_REASON VARCHAR(200)` 컬럼 추가 + `applyResolvedToAttr` 에서 함께 저장 |
| 4-3 | **ERwin 임포트 진입 경로 확인** | 컨트롤러/파서(ErwinXmlParser)는 존재하나 화면 버튼 경로 미확인 | DSDatamodelCollection.vue 또는 DSDatamodelStatus.vue 에서 ERwin XML 업로드 버튼 노출 여부 확인. 없다면 추가 |

---

## 5. 요구사항 충족률 개선 [출처: 47번 §4]

전체 29항목 중 미충족 3개. 영향도 순으로:

| # | 항목 | 우선도 | 현재 상태 | 비고 |
|---|------|--------|----------|------|
| 5-1 | **논리→물리 자동 변환** | 높음 | Phase A/B 구현 완료 → 47번 표 `❌ → ✅` 로 업데이트 필요 | 문서만 갱신 |
| 5-2 | **실시간 동기화 (진단 스케줄러)** | 중간 | 36번에 설계만 있음, 구현 X | 우선순위 확인 필요 |
| 5-3 | **ETL 연계 가이드 문서** | 낮음 | API 공개 완료. 연동 예시 문서만 부재 | 외부 ETL(Talend/NiFi) 연결 예시 가이드 한 장 |

### 5-4. DBMS별 메타 수집 커버리지 [출처: 47번 §1]

인덱스/제약조건 Named Query 추가 구현 대상:

| DBMS | 테이블/컬럼 | 인덱스 | 제약조건 | 상태 |
|------|-----------|--------|---------|------|
| Oracle | ✅ | ✅ | ✅ | 완비 |
| MariaDB | ✅ | ❌ | ❌ | 인덱스/제약조건 Named Query 작성 필요 |
| Cubrid | ✅ | ❌ | ❌ | 동일 |
| PostgreSQL | ❌ | ❌ | ❌ | 전체 미구현 (내부 DB용으로만 사용 중) |

**작업 위치**: `q-common/src/main/resources/mapper/stnd/datamodel.xml` 의 `selectSchemaListSql` 옆에 DBMS별 분기 추가 + `collectIndex`/`collectConstraint` 확장

---

## 6. 테스트 자동화 [출처: 50번 §4-8 체크리스트 8]

| # | 항목 | 파일 |
|---|------|------|
| 6-1 | Selenium Step 4-5 (컬럼 추가 + 표준 변환) 재작성 — 현재 옛날 다이얼로그 구조(용어·도메인 autocomplete) 기준이라 fail | `dataQ설계/테스트/selenium/test_logical_model_e2e.py` |
| 6-2 | Step 6 (DDL 다운로드) 동작 재검증 — 신규 엔드포인트/응답 포맷 유지 확인 | 동 상 |
| 6-3 | 비표준 저장 케이스 테스트 추가 — [비표준으로 추가] 클릭 후 그리드 빨간 배경 확인 | 신규 시나리오 |

---

## 7. 문서 정합성 정리 [출처: 58번, 47번]

| # | 항목 | 조치 |
|---|------|------|
| 7-1 | **58번 절차서의 경로·JDK 파일명** | 현 PC 기준으로는 `C:\Program Files\Java\jdk-1.8`. 58번은 `jdk1.8.0_202` 로 기재. PC별 섹션 추가 or PC별 프로파일 표 분리 |
| 7-2 | **58번의 사용자 경로** | `C:\Users\장재영\...` (PC2) ↔ `C:\Users\naraedata\...` (PC1). 경로 변수화 or PC 구분 |
| 7-3 | **47번 표 §2 논리→물리 자동 변환** | `❌ → ✅` 업데이트 (50번에서 구현됨) |
| 7-4 | **44번 설계서** | 50번 반영 전 상태라 상단에 deprecated 안내 or 내용 리라이트 |

---

## 8. 우선순위 요약 (제안)

```
P0 (오늘/다음 기동 직후)
  1-1 ~ 1-5  컬럼 추가 동작 검증

P1 (이번 주 세션)
  2-1 ~ 2-4  Phase C (용어 등록 연동)
  4-2         변환 사유 영속화 (컬럼 재조회 시 사유 유지)
  6-1 ~ 6-3  Selenium 재작성

P2 (다음 스프린트)
  4-1         인덱스/제약조건 편집 UI
  3-1         일괄 재변환 UX 점검
  5-4         MariaDB/Cubrid 인덱스·제약조건 수집 확장

P3 (문서·설계만)
  3-2, 7-1~7-4  문서 정합성 (44/47/58)
  5-1~5-3      요구사항 충족률 갱신 + 가이드
```

---

## 9. 참조

- [50_논리모델_컬럼추가_분석및개선계획.md](50_논리모델_컬럼추가_분석및개선계획.md) §4-7, §4-8, §5 미구현, §체크리스트
- [56_DataQ_데이터모델관리_현재기능_심층분석.md](56_DataQ_데이터모델관리_현재기능_심층분석.md) §12 미정리 지점
- [57_INDEX_CONSTRAINT_CLCT폐기_정리.md](57_INDEX_CONSTRAINT_CLCT폐기_정리.md) (완료)
- [47_데이터관리SW_요구사항_충족현황.md](47_데이터관리SW_요구사항_충족현황.md) §4 미충족 항목
- [58_서버_빌드_기동_절차.md](58_서버_빌드_기동_절차.md)
- [44_데이터모델_CLCT폐기_재설계.md](44_데이터모델_CLCT폐기_재설계.md) (deprecated 후보)
- [36_진단스케줄러_설계.md](36_진단스케줄러_설계.md) (미구현)
