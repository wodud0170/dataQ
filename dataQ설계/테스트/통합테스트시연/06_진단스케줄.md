# 06. 그룹 5 — 진단 스케줄 (Phase 2~4) (4건)

cron 기반 정기 진단. 약 5분 소요. test_phase4_schedule_coverage 가 무거움 (~218초).

---

## 6-1. test_phase2_schedule_exec.py

| 항목 | 내용 |
|------|------|
| 검증 | 스케줄 트리거 + 실행 (8 step) — cron 발사 → 진단 job 등록 → 결과 row |
| 사전 조건 | dataq-db 정상, 모델 1건 이상 |
| 실행 시간 | ~6초 |
| 시연 포인트 | API 로 cron 표현식 등록 → 다음 발사 시점 미리 계산 → 강제 트리거 → 결과 row 생성 |

```bash
python "dataQ설계/테스트/selenium/test_phase2_schedule_exec.py"
```

---

## 6-2. test_phase3_schedule_ui.py

| 항목 | 내용 |
|------|------|
| 검증 | 스케줄 관리 UI (8 step) — 그리드 / 등록 모달 / 수정 / 삭제 |
| 실행 시간 | ~38초 |
| 시연 포인트 | 진단 스케줄 메뉴 → [등록] → 모델 / 진단 유형 / cron 입력 → 그리드 갱신 |

```bash
python "dataQ설계/테스트/selenium/test_phase3_schedule_ui.py"
```

---

## 6-3. test_phase4_schedule_coverage.py

| 항목 | 내용 |
|------|------|
| 검증 | 스케줄 커버리지 — 7가지 흐름 (9 step), 다양한 cron 패턴, 동시 실행 SKIP 정책 |
| 실행 시간 | ~218초 (~3.5분) |
| 시연 포인트 | 65번의 SKIP 정책 — 같은 모델 동시 실행 차단. 7개 cron 패턴 (매 5분 / 매시 / 매일 22시 / ...) |

```bash
python "dataQ설계/테스트/selenium/test_phase4_schedule_coverage.py"
```

가장 묵직한 단일 테스트. 통과 = 스케줄러 안정성 입증.

---

## 6-4. test_phase4_ui_admin_gate.py

| 항목 | 내용 |
|------|------|
| 검증 | 관리자만 등록/수정/삭제 가능, 일반 사용자는 조회만 (4 step) |
| 사전 조건 | space=관리자 / jyjang=일반 |
| 실행 시간 | ~11초 |
| 시연 포인트 | jyjang 로그인 → 스케줄 화면 → [등록] 버튼 비활성. space 로그인 → 모든 액션 활성 |

```bash
python "dataQ설계/테스트/selenium/test_phase4_ui_admin_gate.py"
```

---

## 그룹 합계 — 4/4 PASS / ~273초

다음: [07_시연시나리오.md](07_시연시나리오.md) — 외부 시연용 단축 흐름
