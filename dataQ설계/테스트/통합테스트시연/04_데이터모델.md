# 04. 그룹 3 — 데이터 모델 (Phase 5 + attr/cascade) (11건)

53번 설계 기반 그리드 편집 + 엑셀 업로드 + 객체 cascade. 약 9분 소요.

---

## 4-1. test_53_grid_add_save.py

| 항목 | 내용 |
|------|------|
| 검증 | 컬럼 그리드 인라인 행 추가 → 입력 → 저장 |
| 실행 시간 | ~38초 |
| 시연 포인트 | 데이터 모델 → 컬럼 → [+] → 한글명/영문명 입력 → [저장] |

---

## 4-2. test_53_paste.py

| 항목 | 내용 |
|------|------|
| 검증 | 엑셀에서 다중 셀 복사 → 그리드 paste → 자동 행 생성 |
| 실행 시간 | ~22초 |
| 시연 포인트 | TSV 클립보드 → Ctrl+V → 행 N개 동시 생성 |

---

## 4-3. test_53_resolve_trigger.py

| 항목 | 내용 |
|------|------|
| 검증 | 표준화 진단 결과 → [해결] 버튼 클릭 → 자동 표준화 추천 모달 진입 |
| 실행 시간 | ~20초 |
| 시연 포인트 | 진단 결과 행 → [해결] → 추천 모달 |

---

## 4-4. test_53_upload_attrs.py

| 항목 | 내용 |
|------|------|
| 검증 | 컬럼 일괄등록 — 양식 다운로드 → 엑셀 업로드 → preview → commit |
| 실행 시간 | ~23초 |
| 시연 포인트 | **이번 세션 변경: 정적 XLSX 양식 (이전 POI 동적 → resources/templates/attrs_template.xlsx 서빙)** |

---

## 4-5. test_53_upload_tables.py

| 항목 | 내용 |
|------|------|
| 검증 | 테이블 일괄등록 — 동일 흐름 |
| 실행 시간 | ~22초 |
| 시연 포인트 | 정적 XLSX `tables_template.xlsx` |

---

## 4-6. test_attr_display_and_resolve_btn.py

| 항목 | 내용 |
|------|------|
| 검증 | 컬럼 한글명/영문명 표시 + [해결] 버튼 활성 조건 (6 step) |
| 실행 시간 | ~20초 |
| 시연 포인트 | 그리드 컬럼별 [해결] 버튼이 표준화 미준수일 때만 활성 |

---

## 4-7. test_attr_update_preserves_meta.py

| 항목 | 내용 |
|------|------|
| 검증 | 컬럼 수정 시 다른 메타(타입/길이/PK여부 등) 보존 |
| 실행 시간 | ~9초 |
| 시연 포인트 | 한 필드만 변경 → 나머지는 무손상 |

---

## 4-8. test_obj_rename_cascade.py

| 항목 | 내용 |
|------|------|
| 검증 | 객체명(테이블명) 변경 시 진단 결과 / 이슈 추적 cascade |
| 실행 시간 | ~39초 |
| 시연 포인트 | 테이블명 변경 → 진단 결과 그리드의 OBJ_NM 도 자동 갱신 |

---

## 4-9. test_cascade_and_word_first.py

| 항목 | 내용 |
|------|------|
| 검증 | 단어 변경 → 용어 / 도메인 / 데이터 모델 cascade (12 step) |
| 실행 시간 | ~150초 |
| 시연 포인트 | 단어 영문약어 수정 → 그 단어를 사용하는 용어/도메인이 자동으로 업데이트 → 모델 표준화 진단 결과도 변동 |

---

## 4-10. test_ca8858d_clsf_domain.py

| 항목 | 내용 |
|------|------|
| 검증 | 자동 표준화 추천 → 수정 모달 → 형식단어 autocomplete + 도메인 cascade (10 step) |
| 실행 시간 | ~23초 |
| 시연 포인트 | **이번 세션 변경: "분류어" → "형식단어" 행안부 용어 통일.** 모달 placeholder/버튼 모두 형식단어 |

---

## 4-11. test_reject_physical_delete.py

| 항목 | 내용 |
|------|------|
| 검증 | 결재 반려 + 물리 삭제 (6 step) |
| 실행 시간 | ~104초 |
| 시연 포인트 | 결재함에서 [반려] → 변경분 미반영. 관리자 [물리 삭제] → DB 에서 row 제거 |

---

## 그룹 합계 — 11/11 PASS / ~470초

다음 그룹: [05_논리물리모델.md](05_논리물리모델.md)
