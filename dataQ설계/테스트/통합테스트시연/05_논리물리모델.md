# 05. 그룹 4 — 논리/물리 모델 진단 (3건)

논리 모델 처리 분기 + E2E + 비표준 컬럼 진단 포함. 약 2분 소요.

---

## 5-1. test_logical_model_diag_handling.py

| 항목 | 내용 |
|------|------|
| 검증 | 논리 모델 (MODEL_TYPE='LOGICAL') 진단 처리 — STRUCT_DIAG_YN 분기 + 진단 비활성 (5 step) |
| 사전 조건 | LOGICAL 타입 모델 + PHYSICAL 타입 모델 둘 다 존재 |
| 실행 시간 | ~16초 |
| 시연 포인트 | LOGICAL 모델은 구조 진단 대상에서 제외. UI 에서 비활성/스킵 표시 |

```bash
python "dataQ설계/테스트/selenium/test_logical_model_diag_handling.py"
```

---

## 5-2. test_logical_model_e2e.py

| 항목 | 내용 |
|------|------|
| 검증 | 논리 모델 E2E — 등록 → 컬럼 추가 → 저장 → 진단 → 결과 확인 |
| 사전 조건 | space 권한 |
| 실행 시간 | ~47초 |
| 시연 포인트 | 데이터 모델 관리에서 [논리 모델 신규] → 컬럼 추가 → 저장 → 진단 메뉴에서 결과 표시 |

```bash
python "dataQ설계/테스트/selenium/test_logical_model_e2e.py"
```

---

## 5-3. test_diag_includes_nonstandard.py

| 항목 | 내용 |
|------|------|
| 검증 | 표준화 진단 결과에 비표준 컬럼 포함 (이전엔 표준만 표시했던 회귀 fix) |
| 실행 시간 | ~9초 |
| 시연 포인트 | 진단 결과에 비표준(영문명 미설정 등) 컬럼이 빨간 배지로 표시 |

```bash
python "dataQ설계/테스트/selenium/test_diag_includes_nonstandard.py"
```

---

## 그룹 합계 — 3/3 PASS / ~72초

다음 그룹: [06_진단스케줄.md](06_진단스케줄.md)
