# 취약점 조치 체크리스트

**작성일:** 2026-04-16

## Critical (즉시 조치)

- [ ] **C-01** WebSecurityConfiguration - API 엔드포인트 인증 적용
  - `permitAll()` → `authenticated()` 변경
  - `/api/admin/**` → `hasRole("ADMIN")`
- [ ] **C-02** LoginController - 사용자 관리 API 인가 추가
  - createUser, removeUser, updateUser, getUserList에 관리자 권한 체크
  - changePassword에 본인 확인 로직 추가
  - createAdmin은 초기 설정 시에만 허용
- [ ] **C-03** WebResourceConfig - CORS 허용 출처 제한
  - `allowedOriginPatterns("*")` → 운영 도메인 명시
- [ ] **C-04** application.properties - DB 자격증명 환경변수화
  - `${DB_USERNAME}`, `${DB_PASSWORD}` 사용
- [ ] **C-05** 프론트엔드 Base64 인코딩 제거 + HTTPS 적용

## High (1-2주 내 조치)

- [ ] **H-01** WebSecurityConfiguration - CSRF 보호 활성화
- [ ] **H-02** BoardController - 파일 업로드 경로 검증
  - `getFileName()` + 특수문자 제거
- [ ] **H-03** BoardController - 파일 다운로드 경로 검증
  - `normalize()` + `startsWith(UPLOAD_DIR)` 체크
- [ ] **H-04** 파일 업로드 타입 화이트리스트 적용
- [ ] **H-05** main.js - innerHTML → textContent 변경
- [ ] **H-06** LoginController - GET → POST 메서드 변경
- [ ] **H-07** SessionService - BCrypt 해싱으로 전환
- [ ] **H-08** DataModelController - CRUD 인가 검증 추가

## Medium (1개월 내 조치)

- [ ] **M-01** 세션 관리 활성화 (maximumSessions, 세션 고정 방어)
- [ ] **M-02** logging-config-prod.xml - 로그 레벨 INFO로 변경
- [ ] **M-03** 민감정보 로깅 제거 (AdminVo toString 필터링)
- [ ] **M-04** 에러 응답에서 e.getMessage() 제거 → 일반 메시지 반환
- [ ] **M-05** @ControllerAdvice 글로벌 예외 핸들러 추가
- [ ] **M-06** ExcelUploadService - XXE 방어 설정 추가
- [ ] **M-07** 구버전 라이브러리 업그레이드 (Jackson, Xerces, Spring Session)
- [ ] **M-08** 로그인 브루트포스 방어 강화
- [ ] **M-09** DTO 패턴 적용 (AdminVo 직접 바인딩 방지)

## Low

- [ ] **L-01** vue.config.js - `productionSourceMap: false` 설정
